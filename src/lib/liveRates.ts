/**
 * หมุดสำหรับวาง "อัตราดอกเบี้ยเงินฝาก" ลงในหน้าเนื้อหา
 *
 * ปัญหาที่แก้: หน้าเงินรับฝากต้องโชว์อัตราดอกเบี้ยเงินฝาก ซึ่งเป็นข้อมูลชุดเดียวกับ
 * ที่ขึ้นหน้าแรก · ถ้าให้เจ้าหน้าที่พิมพ์ซ้ำลงในหน้า พอปรับดอกเบี้ยทีจะต้องแก้สองที่
 * แล้ววันหนึ่งจะลืม — หน้าเว็บโกหกสมาชิกเรื่องดอกเบี้ยคือความผิดพลาดที่รับไม่ได้
 *
 * วิธีทำ: ในเนื้อหาหน้าใส่ `<div class="live-deposit-rates"></div>` ไว้เป็นหมุด
 * แล้วตอน render ค่อยผ่าหน้าออกเป็นก่อน/หลังหมุด แล้ววาง `<DepositRates>` คั่นตรงกลาง
 * (ดู src/app/[...slug]/page.tsx) · แก้ดอกเบี้ยที่เดียว เปลี่ยนทั้งหน้าแรกและหน้านี้
 *
 * ⚠️ ตอนแรกทำเป็นการแทนที่ด้วยสตริง HTML ตรง ๆ ซึ่งง่ายกว่า แต่พอเจ้าของเว็บขอให้
 * **กดสลับกลุ่มและเลื่อนเองได้** ก็ต้องมี state ฝั่งเบราว์เซอร์ สตริงทำไม่ได้
 * จึงเปลี่ยนมาเป็นผ่าหน้าแล้ววางคอมโพเนนต์จริงแทน (22 ส.ค. 2026)
 *
 * ⚠️ ใช้ **class** ไม่ใช่ `data-*` เพราะ `ALLOWED_ATTRS` ใน pageHtml.ts ยอมให้ `div`
 * มีแค่ `class` กับ `data-title` — ใส่ `data-live` ไว้จะโดนตัวกรองกินทันทีที่มีคนกดบันทึก
 * แล้วตารางจะหายไปเงียบ ๆ · และต้องมีชื่อคลาสนี้ใน `ALLOWED_CLASSES` ด้วย
 *
 * ⚠️ หมุดนี้ในหน้าแก้ไข (EditUI) จะกลายเป็นก้อน "โค้ด HTML" — ตั้งใจ
 * เจ้าหน้าที่ย้ายตำแหน่งหรือลบทิ้งได้ แต่ไม่ต้องไปพิมพ์ตัวเลขในนั้น
 */
export const LIVE_DEPOSIT_RATES = "live-deposit-rates";
export const LIVE_LOAN_RATES = "live-loan-rates";

/**
 * ผ่าเนื้อหาออกเป็น ก่อนหมุด / หลังหมุด — ไม่มีหมุดก็คืนทั้งก้อนเป็น `before`
 * แล้ว `after` เป็น null เพื่อให้ฝั่งเรียกรู้ว่าไม่ต้องวางคอมโพเนนต์
 *
 * รับช่องว่างและลำดับแอตทริบิวต์ที่ต่างกันได้ เผื่อ HTML ถูกจัดรูปแบบใหม่ตอนบันทึก
 */
export function splitAtRates(html: string, cls: string): { before: string; after: string | null } {
  const found = html.match(new RegExp(`<div[^>]*class="[^"]*${cls}[^"]*"[^>]*>\s*</div>`));
  if (!found || found.index === undefined) return { before: html, after: null };
  return {
    before: html.slice(0, found.index),
    after: html.slice(found.index + found[0].length),
  };
}
