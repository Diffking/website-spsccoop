import { redirect } from "next/navigation";

/**
 * ที่อยู่เดิมของหน้าติดต่อเรา — ย้ายไปอยู่ใต้ "เกี่ยวกับสหกรณ์" แล้ว
 *
 * ไม่ลบทิ้งเพราะลิงก์ที่แชร์ออกไปก่อนหน้านี้ (คิวอาร์ที่สแกนไปแล้ว ข้อความในไลน์)
 * ยังชี้มาที่นี่อยู่ — ให้พามาที่อยู่ใหม่แทนที่จะเจอ 404
 */
export default function ContactRedirect() {
  redirect("/about/contact/");
}
