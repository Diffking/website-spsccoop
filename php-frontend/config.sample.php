<?php
/*
 * กันเปิดไฟล์นี้ตรง ๆ ผ่านเบราว์เซอร์ — .htaccess กันไว้ชั้นหนึ่งแล้ว
 * แต่โฮสต์บางเจ้าไม่อ่าน .htaccess จึงกันซ้ำในไฟล์เองด้วย (ในนี้มีรหัสลับ)
 */
if (PHP_SAPI !== 'cli' && realpath((string) ($_SERVER['SCRIPT_FILENAME'] ?? '')) === __FILE__) {
    http_response_code(404);
    exit;
}
/**
 * ค่าตั้งของตัวมิเรอร์ — ก๊อปไฟล์นี้เป็น config.php แล้วแก้ค่า
 * config.php จริงไม่ถูกเก็บใน git (มีรหัสลับ)
 */
return [
    // ที่อยู่ของหลังบ้าน (เครื่องที่รันเว็บจริง) — ห้ามใส่ / ปิดท้าย
    'backend' => 'https://coopsmile.org',

    /*
     * โดเมนที่สมาชิกเปิดจริง — ส่งไปบอกหลังบ้านทุกครั้งที่ไปขอหน้า
     * หลังบ้านใช้ตัดสินว่าหน้านี้ให้กูเกิลเก็บได้ไหม (โดเมนสำรองห้ามเก็บ กันเนื้อหาซ้ำ)
     */
    'public_host' => 'www.spsccoop.com',

    // โฟลเดอร์เก็บแคช ต้องเขียนได้ (chmod 755 หรือ 775)
    'cache_dir' => __DIR__ . '/cache',

    // หน้าเว็บเก็บไว้กี่วินาทีก่อนไปถามหลังบ้านใหม่
    'ttl_page' => 120,
    // ไฟล์แนบ/รูป/ไฟล์ประกอบเว็บ แทบไม่เปลี่ยน เก็บได้ยาว
    'ttl_asset' => 604800,

    // รหัสลับสำหรับสั่งล้างแคชจากหลังบ้าน (ตั้งให้ตรงกันสองฝั่ง)
    'purge_token' => 'เปลี่ยนเป็นรหัสยาว ๆ ของคุณเอง',

    // เวลารอหลังบ้านสูงสุด (วินาที) — เกินนี้ถือว่าหลังบ้านไม่ว่าง ใช้ของในแคชแทน
    'timeout' => 5,

    /*
     * เครื่องที่สำนักงานเปิดแค่ จ-ศ 08:30-16:30 นอกเวลานั้นติดต่อไม่ได้เป็นปกติ
     * พอเจอว่าติดต่อไม่ได้ ให้จำไว้กี่วินาทีก่อนจะลองใหม่ — ระหว่างนั้นยกของในแคชให้ทันที
     * ไม่ต้องให้คนอ่านเว็บนั่งรอ timeout ทีละหน้า
     */
    'down_ttl' => 60,

    // ตอนอุ่นแคชไม่มีใครนั่งรออยู่ รอไฟล์ใหญ่ได้นานกว่าปกติ (วินาที)
    'warm_timeout' => 120,

    // ตอนอุ่นแคช ให้ดึงรูป/ไฟล์แนบ/ไฟล์ประกอบเว็บมาเก็บด้วยไหม
    // (ต้องเปิด ไม่งั้นกลางคืนหน้าเว็บขึ้นแต่รูปแตกและปุ่มกดไม่ทำงาน)
    'warm_assets' => true,
    // เพดานพื้นที่ที่ยอมให้การอุ่นแคชดึงไฟล์ลงมาต่อรอบ (เมกะไบต์)
    'warm_budget_mb' => 500,

    /*
     * ที่อยู่ที่ห้ามส่งต่อเด็ดขาด — หลังบ้านกับระบบล็อกอิน
     * หลังบ้านมีการล็อกด้วยชื่อโดเมนอยู่แล้ว ตรงนี้คือกันอีกชั้นที่ฝั่งโฮสต์
     */
    'blocked' => ['/admin', '/api/admin', '/api/auth'],
];
