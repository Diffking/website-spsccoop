# AGENTS.md

ข้อควรรู้ก่อนแก้อะไรในโปรเจกต์นี้ — เรื่องที่ดูจากโค้ดอย่างเดียวไม่รู้

## ⚠️ เครื่องพัฒนาเครื่องนี้คือเครื่องที่เสิร์ฟเว็บจริง

`docker compose` ในโฟลเดอร์นี้รัน **เว็บจริงของ spsccoop.org** อยู่ (service `cloudflared` ต่อ tunnel
เข้า `web`) ไม่ใช่ของทดลอง คำสั่งที่ห้ามสั่งเล่น:

- `docker compose down -v` — ลบ volume `pgdata` = ข้อมูลจริงหายถาวร ไม่มีเครื่องสำรอง
- `docker image prune` — ลบ image เก่าที่ใช้ถอยกลับตอน deploy พัง
- `docker compose stop cloudflared` — เว็บจริงหลุดจากโดเมนทันที (ใช้ตอนตั้งใจปิดเท่านั้น)

## ⚠️ โฮสต์ spsccoop.com แบนไอพีเราได้ — เรื่องนี้สำคัญที่สุด

โฮสต์ (hostinglotus) มีไฟร์วอลล์ที่ไวมาก **19 ส.ค. 2026 การสั่งลบไฟล์ผ่าน FTP ทีละไฟล์
หลายพันครั้งติดกัน ทำให้ไอพีสำนักงาน `183.89.228.246` โดนแบนทุกพอร์ต** (21/80/443/2082/2083)
กว่าจะกลับมาใช้ได้ต้องแจ้งบริษัทปลดให้ — ระหว่างนั้นทำอะไรกับโฮสต์ไม่ได้เลย

### กฎที่ต้องทำตาม

| งาน | ห้าม | ให้ทำแทน |
|---|---|---|
| ลบโฟลเดอร์ทั้งก้อน | ลบผ่าน FTP ทีละไฟล์ | File Manager ใน cPanel — คำขอเดียวจบ |
| อัปไฟล์จำนวนมาก (เช่น `uploads/` 260 ไฟล์) | อัปทีละไฟล์ผ่าน FTP | บีบเป็น zip ก้อนเดียวแล้วแตกด้วย File Manager |
| ตรวจสอบเว็บ | ยิงหลายคำขอรัว ๆ | ทีละคำขอ เว้นจังหวะ (เคยโดน 429 ตอนยิง 6 ครั้งติด) |
| ทดสอบระบบที่ทำงานผ่านโดเมนสาธารณะ | ยิงซ้ำ ๆ ไปที่ `www.spsccoop.com` | ยิงที่ `spsccoop.org` แล้วใส่หัวคำขอเอาเอง |

**ระบบเองส่งไฟล์ขึ้น FTP ไม่ได้แล้ว** — ถอด `src/lib/ftp.ts` ออกทั้งไฟล์เมื่อ 21 ส.ค. 2026
(ดูกฎข้อ 2) ไฟล์ที่อัปในหลังบ้านเก็บลง `uploads/` ของเครื่องนี้อย่างเดียว
ที่อันตรายคือ **สคริปต์ที่วนลูปหลายร้อยหลายพันครั้ง**

### กฎ: อย่าใช้โดเมนโฮสต์เป็นสนามทดสอบ

**20 ส.ค. 2026 ยิงทดสอบ `POST /api/track/` ผ่าน www.spsccoop.com ประมาณ 8 ครั้งใน 1 วัน
ไอพีสำนักงาน `180.180.230.61` โดนไฟร์วอลล์เซิร์ฟเวอร์ทิ้งแพ็กเก็ตทุกพอร์ต** ทั้ง ping ทั้ง
cPanel ทั้ง FTP · เน็ตสำนักงานเป็นไอพีคงที่ รีสตาร์ตเราเตอร์ไม่ช่วย ต้องรอบริษัทปลดอย่างเดียว

ตัวมิเรอร์บอกหลังบ้านทุกอย่างผ่านหัวคำขอ **เราใส่หัวเองก็ได้ผลเหมือนกันเป๊ะ**
โดยไม่ต้องแตะโฮสต์เลยสักครั้ง

```bash
# ❌ ผิด — ยิงผ่านโฮสต์
curl -X POST https://www.spsccoop.com/api/track/ -d '{"path":"/"}'

# ✅ ถูก — ยิงที่เครื่องเราเอง สวมบทเป็นตัวมิเรอร์
curl -X POST https://spsccoop.org/api/track/ \
  -H 'Content-Type: application/json' \
  -H 'X-Public-Host: www.spsccoop.com' \
  -H 'X-Visitor-Ip: 203.0.113.9' \
  -d '{"path":"/tmp/track-check"}'
```

ยิงผ่านโฮสต์จริงได้เฉพาะตอนต้องพิสูจน์ว่า**ไฟล์บนโฮสต์**ทำงาน (เช่น เพิ่งอัป `index.php` ใหม่)
— **ครั้งเดียวพอ** แล้วเช็คผลที่ฐานข้อมูลแทนการยิงซ้ำ

ทดสอบเสร็จอย่าลืมลบแถวที่เกิดจากการทดสอบออกจาก `PageView` / `VisitorDay` ไม่งั้นยอดจริงเพี้ยน

### สำนักงานมีเน็ต 2 ค่าย — 20 ส.ค. 2026 โดนแบนครบทั้งคู่

สลับด้วยมือ ไม่ใช่ dual-WAN อัตโนมัติ (เสียบสายเข้าการ์ดไหน ก็ออกเส้นนั้น)

| ไอพี | ค่าย | บล็อก | เปลี่ยนได้ไหม | โดนแบน | เพราะอะไร |
|---|---|---|---|---|---|
| `183.89.228.246` | 3BB | `183.89.0.0/16` TTBP-TH | ❌ **บริษัทกำหนดมา** รีสตาร์ตไม่ช่วย | 19 ส.ค. 2026 | ลบไฟล์ผ่าน FTP ทีละไฟล์หลายพันครั้ง |
| `180.180.230.61` | TOT | `180.180.0.0/16` TOT-AS-AP | ✅ เปลี่ยนเอง | 20 ส.ค. 2026 | ยิงทดสอบ `POST /api/track/` ~8 ครั้ง |
| `182.53.222.44` | TOT (เส้นเดิม ไอพีใหม่) | `182.52.0.0/15` TOT-AP | ✅ เปลี่ยนเอง | 21 ส.ค. 2026 | FTP หนึ่งไฟล์ + ล้างแคช + เปิดเว็บตรวจ รวมไม่ถึง 10 คำขอ |

**21 ส.ค. 2026 เผาเส้นที่สามแล้ว** — วันนั้นแตะโฮสต์แค่ FTP อัป `config.php` หนึ่งไฟล์
(list + get + put + get) ตามด้วยล้างแคชหนึ่งครั้งและเปิดหน้าเว็บตรวจอีกไม่กี่ครั้ง
**รวมทั้งวันไม่ถึง 10 คำขอ ก็โดนแล้ว** — ไฟร์วอลล์ตัวนี้ไวกว่าที่เคยประเมินไว้มาก
อย่าคิดว่า "แค่ไม่กี่ครั้งคงไม่เป็นไร" · แตะโฮสต์ทีเดียวให้จบงาน แล้วเลิกยุ่งทั้งวัน
ที่แย่กว่าคือ **ตัวอุ่นแคชยิงจากไอพีนี้เอง** พอโดนแบนแล้วมันจะยิงซ้ำ
เติมตัวนับให้ CSF/LFD เอง — ถ้ารู้ว่าโดนแบนและยังไม่ได้ปลด ให้ `docker compose stop mirror-warm`
ไว้ก่อน แล้วค่อยเปิดกลับตอนได้ไอพีที่ใช้ได้

### ติดต่อโฮสต์ยังไง — HostingLotus กับ Metrabyte คือบริษัทเดียวกัน

ท้ายเว็บ hostinglotus.com เขียนว่า **บริษัท เมทราไบต์ คลาวด์ จำกัด** ที่อยู่ 72 อาคาร CAT TOWER ชั้น 18 บางรัก ตรงกับเจ้าของบล็อกไอพีใน RDAP เป๊ะ (`119.59.96.0/19` METRABYTE-TH) — **ไม่ใช่รีเซลเลอร์เช่าเครื่องกัน**

| ช่องทาง | ค่า |
|---|---|
| อีเมลหลัก | `support@hostinglotus.com` |
| อีเมลสำรอง | `support@metrabyte.cloud` |
| แจ้งเหตุละเมิด | `abuse@metrabyte.cloud` |
| โทร | 02-1054-322 · 02-026-3124 |
| LINE | <https://page.line.me/dyr2931w> |

⚠️ **`hostinglotus.net` ถูกระงับไปแล้ว** (ขึ้น "This account has been suspended") เว็บจริงคือ **`.com`** · อย่าส่งอีเมลไป `@hostinglotus.net` และอย่าเอา `hostinglotus.net` เปิดไม่ได้ไปสรุปว่าโดนแบนทั้งเครือข่าย

เครื่องที่เก็บเว็บสหกรณ์คือ **`119.59.120.4`** — `spsccoop.com` · `www` · `ftp` ชี้มาที่นี่ทั้งหมด ส่วน `hostinglotus.net` อยู่ที่ `119.59.106.88` คนละเครื่อง จึงใช้เทียบตอน ping หาสาเหตุได้

**สองเส้นนี้ใช้คนละหน้าที่กัน — อย่าเอามาใช้สลับกัน**

- **3BB `183.89.228.246` เป็นไอพีที่บริษัทกำหนดมาให้ ไม่มีวันเปลี่ยน** รีสตาร์ตเราเตอร์
  ไม่ช่วยอะไรเลย · แต่ข้อดีคือ **เอาไปขอ allow list ถาวรกับ hostinglotus ได้จริง**
  เพราะเลขไม่มีวันเปลี่ยน ขอครั้งเดียวจบ — นี่คือเส้นที่ควรใช้ทำงานกับโฮสต์ระยะยาว
- **TOT เปลี่ยนได้** จึงเป็นเส้นสำรองไว้ใช้ตอนโดนแบนแล้วมีงานด่วนต้องทำ
  ขอ allow list ให้ไม่ได้เพราะเลขเปลี่ยนไปเรื่อย ๆ

### ⭐ TOT ไม่ได้เป็นไอพีคงที่ — รีสตาร์ตเราเตอร์ได้ไอพีใหม่

**21 ส.ค. 2026 ตรวจ RDAP พบว่าไอพีขาออกคือ `182.53.222.44` อยู่ในบล็อก
`182.52.0.0 – 182.53.255.255` ของ TOT** ทั้งที่เอกสารนี้บันทึกไว้ว่าเส้น TOT
คือ `180.180.230.61` แบบคงที่ · แปลว่า **ไอพีของ TOT เปลี่ยนเองได้** ไม่ได้คงที่จริง
(เปลี่ยนไปเองระหว่างบ่ายถึงเย็นวันที่ 20 ส.ค. โดยไม่มีใครทำอะไร)

นี่คือทางออกที่เร็วที่สุดเวลาโดนแบน — **รีสตาร์ตเราเตอร์ TOT แล้วได้ไอพีใหม่ที่ไฟร์วอลล์
ไม่รู้จัก** ใช้งานโฮสต์ได้ทันทีโดยไม่ต้องรอบริษัทปลดให้

ปลอดภัยกับ `cloudflared` เพราะเป็นการเชื่อมต่อขาออก เน็ตกลับมาแล้วมันต่อเองภายใน 1-2 นาที
· ระหว่างนั้นหลังบ้านกับ `www.spsccoop.com` จะสะดุดสั้น ๆ

⚠️ **ไม่ใช่ทางแก้ถาวร** — ไอพีใหม่จะโดนแบนอีกถ้ายังยิงหาโฮสต์รัว ๆ เหมือนเดิม
(21 ส.ค. `182.53.222.44` โดนแบนภายในวันเดียว ทั้งที่แตะไม่ถึง 10 ครั้ง)
ทำแล้วให้รีบทำงานที่ค้างให้จบรอบเดียว แล้วเลิกยุ่งกับโฮสต์ · **ทางแก้จริงคือขอ allow list
ถาวรจาก hostinglotus** ซึ่งยังไม่ได้โทร

เช็คว่าเปลี่ยนแล้วจริงด้วย `curl -s https://api.ipify.org` — ต้องได้เลขที่ไม่ใช่ตัวเดิม

**ทางแก้เดิมที่เขียนไว้ว่า "สลับไปเน็ตอีกเส้น" ใช้ไม่ได้แล้ว — เผาไปครบทั้งสองเส้น**
เวลาแจ้งบริษัทต้องแจ้งปลดพร้อมกันทั้งสองไอพี และขอ allow list ถาวรทั้งคู่
ที่เหลืออยู่ทางเดียวคือ **ฮอตสปอตมือถือ** (CGNAT ไอพีเปลี่ยนทุกครั้งที่ต่อ ไฟร์วอลล์ไม่รู้จัก)
— ต่อแล้ว **อุ่นแคชรอบเดียวจบ อย่ายิงทดสอบซ้ำ** ไม่งั้นเผาไอพีที่สามตามไปอีก

เช็คว่าตอนนี้ออกเส้นไหน — **ทำทุกครั้งหลังสลับสาย** เพราะถ้าเสียบค้างไว้สองเส้น Windows
เลือกให้เองตาม metric แล้วจะนึกว่าสลับแล้วทั้งที่ยังออกเส้นเดิม

```bash
curl -s https://api.ipify.org; echo     # 183.89.x = 3BB · 180.180.x = TOT
netsh interface show interface          # ดูว่าการ์ดไหน Connected
```

**ห้ามยิง warm/purge เพื่อ "ทดสอบว่ายังโดนแบนอยู่ไหม"** — ยิงจากไอพีที่โดนแบนตัน 100%
รู้ผลล่วงหน้าอยู่แล้ว ไม่ได้ข้อมูลใหม่ แถมเติมตัวนับให้ CSF/LFD จนเลื่อนเป็น permanent deny ได้
คำถามเดียวกันตอบได้ฟรีด้วย `ping` (3 แพ็กเก็ต) ตามหัวข้อถัดไป

### เว็บเข้าไม่ได้ = อย่าเพิ่งสรุปว่าโฮสต์ล่ม

อาการโดนแบนหน้าตาเหมือนโฮสต์ล่มทุกอย่าง เช็คจากนอกไอพีเราก่อนเสมอ:

```bash
curl -s "https://r.jina.ai/https://www.spsccoop.com/" | head -5
```

**อย่าใช้ allorigins หรือ codetabs ตัดสิน** — สองตัวนี้เสียของมันเองคืน error 522
เคยอ่านผิดเป็นว่าเว็บล่มทั้งที่เว็บทำงานปกติมาแล้ว

### แยกให้ออกว่าโดนกันที่ไหน — ใช้ ping ตัดสิน

`ping` ไปที่เครื่องที่เก็บเว็บ (`119.59.120.4`) เทียบกับเครื่องอื่นของบริษัทเดียวกัน
(`hostinglotus.net` = `119.59.106.88` คนละเครื่อง เส้นทางเน็ตเดียวกัน)

| ผล | แปลว่า |
|---|---|
| `119.59.120.4` ตอบไม่ได้ · `119.59.106.88` ตอบใน ~18ms | ไฟร์วอลล์ **เครื่องที่เก็บเว็บ** ตั้ง DROP ไอพีเรา |
| ตอบไม่ได้ทั้งคู่ แต่เว็บไทยเจ้าอื่นเปิดได้ | บริษัทบล็อกทั้งเครือข่ายเขา |
| เว็บไทยเจ้าอื่นก็เปิดไม่ได้ | เน็ตฝั่งเรา ไม่เกี่ยวกับโฮสต์ |

**อย่าใช้ `hostinglotus.net` เปิดไม่ได้เป็นตัวตัดสินว่าโดนแบน** — เว็บเขาใบรับรอง SSL เสียเอง
เปิด `https://` แล้วขึ้น `SEC_E_WRONG_PRINCIPAL` เป็นปกติทั้งที่ `http://` ตอบ 200
เคยอ่านผิดเป็น "โดนแบนทั้งเครือข่าย" มาแล้ว 20 ส.ค. 2026

แก้โดยแจ้งบริษัทให้ปลดไอพีออกจาก deny list (CSF/Imunify360) และ**ขอใส่ allow list ถาวร**
เพราะเครื่องนี้ยิงหาโฮสต์อัตโนมัติวันละ 2 ครั้งอยู่แล้ว · ถ้าเน็ตไม่ใช่ไอพีคงที่ รีสตาร์ตเราเตอร์
เอาไอพีใหม่ก็จบ (ปลอดภัยกับ `cloudflared` เพราะเป็นการเชื่อมต่อขาออก ต่อกลับเองเมื่อเน็ตมา)
— 3BB คงที่ **แต่ TOT ไม่คงที่ รีสตาร์ตเราเตอร์ได้ไอพีใหม่** (ดูหัวข้อข้างบน)

### โดนแบนแล้วอะไรพัง อะไรไม่พัง — สมาชิกไม่กระทบ

การแบนกันแค่ทิศทาง **เรา → โฮสต์** ส่วนทิศทางที่ทำให้เว็บมีข้อมูลใหม่คือ **โฮสต์ → เรา**
ซึ่งวิ่งผ่าน Cloudflare คนละทาง ไม่โดนอะไรเลย

```
สมาชิก → spsccoop.com ──ดึงเองทุก 120 วิ──→ Cloudflare → spsccoop.org   ✅ ปกติ
เรา ──สั่งล้างแคช (purge) · สั่งอุ่นแคช (warm)──→ spsccoop.com            ❌ ตัน
```

`php-frontend/lib/mirror.php` โฮสต์เป็นฝ่ายวิ่งมาถามเราเองเมื่อสำเนาหมดอายุ (`ttl_page` 120 วิ)
ไม่ได้รอให้เราไปบอก **ข้อมูลบนหน้าเว็บจึงยังใหม่ตลอด ช้าสุดไม่เกิน 2 นาที** ไม่ได้ล้าหลัง

| สิ่งที่หายไป | ผลจริง |
|---|---|
| ล้างสำเนาทันทีตอนกดบันทึก | ต้องรอสำเนาหมดอายุเอง ≤ 2 นาที |
| อุ่นแคชวันละ 2 ครั้ง (`mirror-warm`) | หน้า/ไฟล์ที่ไม่มีใครเปิด จะไม่มีสำเนาเตรียมไว้ |

**ที่อันตรายจริงคือช่วงปิดเครื่อง** (กลางคืน เสาร์อาทิตย์) โฮสต์ดึงต้นทางไม่ได้ ต้องพึ่งแคชล้วน ๆ
หน้าหรือไฟล์ที่ไม่เคยถูกอุ่นเลยจะเปิดไม่ขึ้น — **ก่อนปิดเครื่องเย็นศุกร์ต้องอุ่นให้ได้ 1 รอบเสมอ**

รอบ 15:30 ของวันศุกร์ครอบให้อยู่แล้ว **ถ้าปิดเครื่องก่อน 15:30 ต้องสั่งอุ่นเอง**

```bash
docker compose exec mirror-warm sh /warm.sh     # รอ log ขึ้น "อุ่นแคช: สำเร็จ"
```

⚠️ **`docker compose restart mirror-warm` ไม่อุ่นทันทีอีกแล้ว** (ตั้งแต่ 21 ส.ค. 2026
ที่เปลี่ยนไปใช้ crond) — restart แค่ล้างตัวนับของตัวตัดวงจร แล้วรอถึงเวลาในตาราง

รูปกับไฟล์แนบไม่ต้องห่วงว่าจะค้างของเก่าตาม `ttl_asset` 7 วัน — ไฟล์ที่อัปใหม่ได้ชื่อ UUID
ใหม่ทุกครั้ง (`src/app/api/admin/upload/route.ts`) คนละที่อยู่กับของเก่า ไม่มีทางชนกัน

## ⭐ กฎ 3 ข้อ: แก้โค้ดแล้วผลไปโผล่ที่ไหน · เมื่อไหร่ถึงแตะโฮสต์

กฎพวกนี้กระจายอยู่หลายหัวข้อในไฟล์นี้ — รวมไว้ตรงนี้ให้อ่านข้อเดียวจบ
(เจ้าของเว็บย้ำเมื่อ 21 ส.ค. 2026 ว่าต้องเข้าใจตรงกันทุกคน)

### กฎข้อ 1 — แก้โค้ดแล้วผลต้องขึ้นที่ `spsccoop.org` ก่อนเสมอ

**ถ้าผลไปโผล่ที่ `.com` ก่อน `.org` = ผิดวัตถุประสงค์ แปลว่ามีคนไปแก้ไฟล์บนโฮสต์ตรง ๆ**

```
เราแก้โค้ด → build → web (เครื่องนี้) → spsccoop.org        ← ผลขึ้นทันที
                                          ↑
                        www.spsccoop.com ─┘ ดึงเองเมื่อสำเนาหมดอายุ ≤ 2 นาที
```

`php-frontend/config.sample.php` ตั้ง `'backend' => 'https://spsccoop.org'` —
**โฮสต์เป็นฝ่ายวิ่งมาถามเราเอง เราไม่เคยเป็นฝ่ายส่งอะไรไปให้**

| แก้ที่ | ผลไปโผล่ที่ | ต้องอัปขึ้นโฮสต์ไหม |
|---|---|---|
| `src/` · `prisma/` · `public/` · `next.config.ts` | **spsccoop.org ทันที** → `.com` ตามมาใน ≤ 2 นาที | ❌ ไม่ต้อง |
| `docker-compose.yml` · `scripts/` | เครื่องนี้เท่านั้น | ❌ ไม่ต้อง |
| **`php-frontend/`** | **www.spsccoop.com เท่านั้น — ไม่มีผลกับ `.org` เลย** | ⚠️ **ต้อง FTP** |

`php-frontend/` **ไม่ใช่ frontend ของเว็บ** แต่เป็นโค้ดของตัวมิเรอร์ที่รันอยู่บนโฮสต์
(`index.php` · `lib/mirror.php` · `warm.php` · `purge.php` · `.htaccess`)
ไม่มีระบบ deploy อัตโนมัติ ต้องอัปเอง — **นี่คือข้อยกเว้นเดียวของกฎข้อ 1**

### กฎข้อ 2 — ระบบส่งไฟล์ขึ้น FTP ไม่ได้อีกแล้ว (ถอดออกทั้งก้อน 21 ส.ค. 2026)

**เจ้าของเว็บสั่งถอด ไม่ใช่แค่ปิดสวิตช์** — ของเดิมปิดด้วย `ASSETS_BASE_URL` ที่คอมเมนต์ไว้
ใน `.env` ซึ่งใครเผลอเอา `#` ออกก็กลับมาส่ง FTP ทันทีโดยไม่มีอะไรเตือน

| ของที่เอาออก | เดิมทำอะไร |
|---|---|
| `src/lib/ftp.ts` | ทั้งไฟล์ — เป็นที่เดียวที่ `import { Client } from "basic-ftp"` |
| `uploadToFtp()` ใน `POST /api/admin/upload` | ส่งไฟล์ที่เพิ่งอัปขึ้น FTP ของโฮสต์ทันที |
| `GET /api/admin/ftp-test` + ปุ่ม "ทดสอบ FTP" | กดทีเดียวยิง ~16 คำขอไปโฮสต์ |
| `basic-ftp` ใน `package.json` | ไลบรารีหายจากโปรเจกต์แล้ว |

ตัวช่วยชื่อโฟลเดอร์ (`FOLDERS` `pageFolder` `cleanPageFolder` `committeeFolder` `isFolder`)
ย้ายไปอยู่ที่ `src/lib/assetFolders.ts` ที่เดียว — **ไฟล์นั้นเป็นข้อมูลล้วน ไม่แตะเครือข่าย**

**ตอนนี้ไฟล์ที่อัปเก็บลง `uploads/` ของเครื่องนี้ที่เดียวเท่านั้น** ไปถึง `www.spsccoop.com`
ทางเดียวคือ **โฮสต์เป็นฝ่ายดึงเอง** ผ่านตัวมิเรอร์ — ดึงตอนมีคนเปิดดู หรือดึงล่วงหน้าตอน
อุ่นแคช 09:30 / 15:30

```
อัปรูป/PDF → uploads/ (เครื่องนี้) ─┬→ โฮสต์ดึงเองตอนสมาชิกเปิด (ttl_asset 7 วัน)
                                    └→ หรือ cron 09:30/15:30 ดึงล่วงหน้าให้
```

⚠️ **ผลที่ตั้งใจให้เป็น: รูปที่เพิ่งอัปจะยังไม่มีบนโฮสต์ทันที** — ปกติโฮสต์ดึงให้เองในไม่กี่วินาที
ตอนมีคนเปิดหน้านั้น **แต่ถ้าอัปของแล้วปิดเครื่องเลยโดยไม่ได้อุ่นแคช ไฟล์ใหม่จะไม่ขึ้นบน `.com`
ตลอดเสาร์อาทิตย์** → อัปเสร็จก่อนปิดเครื่องต้อง `docker compose exec mirror-warm sh /warm.sh`

**ไม่มีงานอัตโนมัติตัวไหนแตะ FTP** — ไม่ว่า `mirror-warm` · `holiday-sync` · `db-backup` ·
การ build · การ deploy · และ **ไม่เคยมีฟังก์ชันลบไฟล์ผ่าน FTP อยู่ในระบบเลย**
ไม่มี `client.remove` ไม่มีลูปไล่ลบ → เหตุ 19 ส.ค. 2026 ที่ทำให้ไอพีโดนแบน
**โค้ดชุดนี้ทำซ้ำไม่ได้ทางเทคนิค**

⚠️ ความเสี่ยงที่เหลือคือ **ฝั่งคนล้วน ๆ** — เปิดโปรแกรม FTP อัป `php-frontend/` ขึ้นโฮสต์เอง
· **จะแตะ FTP หรืออัปอะไรขึ้นโฮสต์ ต้องถามเจ้าของเว็บก่อนทุกครั้ง ห้ามทำเอง**
· **ห้ามเขียนสคริปต์วนลูป FTP ทุกกรณี** (ไล่ลบ · ไล่อัป · ไล่ list ทั้งโฟลเดอร์)
· **ห้ามเอา `uploadToFtp` กลับเข้ามาใหม่** ถ้ามีใครขอให้ "อัปขึ้นโฮสต์ให้เร็วขึ้น"
  ต้องถามเจ้าของเว็บก่อน — เรื่องนี้ตัดสินใจไปแล้ว ไม่ใช่ของค้างที่ยังไม่ได้ทำ

`FTP_*` ที่ยังค้างอยู่ใน `.env` ตอนนี้ **ไม่มีโค้ดตัวไหนอ่านแล้ว** ลบทิ้งได้ถ้าอยากให้สะอาด
(แต่ `.env` เป็นของเจ้าของเว็บ อย่าไปแก้เอง)

### กฎข้อ 3 — ตรวจงานที่ `spsccoop.org` เท่านั้น

```bash
npm run build                        # ผ่านก่อน
docker compose up -d --build web     # เว็บจริงสะดุด ~1-2 นาที
```

แล้วเปิด `https://spsccoop.org` ดูผล — **ห้ามเอา `www.spsccoop.com` มาเป็นตัวตัดสิน
ว่างานเสร็จหรือยัง** เพราะสำเนาบนโฮสต์ตามมาทีหลังตามรอบแคช การไปกดรีเฟรชที่ `.com`
ซ้ำ ๆ เพื่อรอดูผล = แตะโฮสต์ฟรี ๆ หลายครั้งโดยไม่จำเป็น ซึ่งเป็นทางที่เผาไอพีมาแล้ว 3 เส้น

## หน้าเว็บสาธารณะอยู่ที่ www.spsccoop.com แล้ว (ตั้งแต่ 19 ส.ค. 2026)

เครื่องนี้ยังเป็นคนสร้างหน้าเว็บทุกหน้าเหมือนเดิม แต่สมาชิกไม่ได้เข้ามาที่นี่ตรง ๆ แล้ว

```
สมาชิก → www.spsccoop.com (PHP บนโฮสต์ เก็บสำเนา) ──ขอหน้าเว็บ──→ spsccoop.org (เครื่องนี้)
เจ้าหน้าที่ → admin.spsccoop.org (เข้าได้ที่นี่ที่เดียว · เปิดหน้าแรกแล้วพาไปหน้าเข้าสู่ระบบให้เอง)
```

**`spsccoop.org/admin` ตอบ 404 แล้ว** ตั้งแต่ 20 ส.ค. 2026 — ย้ายไปซับโดเมนของตัวเอง
ผ่าน Cloudflare Tunnel ตัวเดิม (`ADMIN_HOST` + `ADMIN_ROOT_HOST` ใน `.env` · ดู `doc/SUBDOMAIN_ADMIN.md`)
ตั้งค่าโดเมนพังเมื่อไหร่ยังเข้าทาง `localhost:8030/admin` ได้เสมอ ไม่ล็อกตัวเองออก
(หน้าเข้าสู่ระบบอยู่ที่ `/login` แล้ว — ดูหัวข้อ "ทางเข้าหลังบ้าน" ด้านล่าง)

**เข้าหลังบ้านไม่ได้แวบเดียวแล้วหายเอง = ปกติ ไม่ใช่เว็บล่ม** — `cloudflared` เปิดไว้ 4 เส้นขนาน
(bkk01 ×2 · sin07 ×2) เส้นใดเส้นหนึ่งหลุดแล้วต่อกลับเองใน ~1 นาที ถ้าจังหวะที่กดพอดีวิ่งเข้าเส้นนั้น
จะเจอค้างสั้น ๆ · ดูย้อนได้จาก `docker compose logs --tail=20 cloudflared` หา `Retrying connection`
แล้วตามด้วย `Registered tunnel connection` · ตอนสลับสายเน็ตก็เจออาการเดียวกัน 1-2 นาทีระหว่างต่อกลับ
เรื่องนี้ **ไม่เกี่ยวกับการโดนแบนไอพี** คนละเส้นทางกัน หลังบ้านเข้าได้เสมอถึงแม้ไอพีจะโดนแบนอยู่

โค้ดฝั่งโฮสต์อยู่ใน `php-frontend/` — แก้แล้วต้องอัปขึ้นโฮสต์เอง ไม่มีระบบ deploy อัตโนมัติ
`_data/spsccoop-frontend/` คือชุดที่พร้อมอัป (ไม่อยู่ใน git เพราะมีรหัสใน config.php)

| ที่อยู่บนโฮสต์ | คืออะไร |
|---|---|
| `.htaccess` | กฎเปิดจริง (สำเนาของ `php-frontend/.htaccess`) |
| `.htaccess.maintenance` · `maintenance.html` | หน้าแจ้งปรับปรุง เก็บไว้ใช้คราวหน้า |
| `cache/` | สำเนาหน้าเว็บ รูป ไฟล์แนบ ~200 MB ลบได้ ระบบสร้างใหม่เอง |

### ยิงหาโฮสต์ไม่สำเร็จ = หยุด ห้ามยิงซ้ำ (ตัวตัดวงจร)

ของสองอย่างที่ยิงหาโฮสต์เอง — ล้างสำเนาตอนกดบันทึก (`src/lib/mirrorPurge.ts`) และ
อุ่นสำเนาตามตาราง (service `mirror-warm`) — **มีตัวตัดวงจรทั้งคู่ตั้งแต่ 21 ส.ค. 2026**

พลาดครั้งเดียวก็หยุดยิงทันที ไม่ลองใหม่จนกว่าจะพ้นเวลาพัก และเวลาพักยาวขึ้นทุกครั้งที่พลาดซ้ำ

| พลาดครั้งที่ | purge (ตอนกดบันทึก) | warm (`scripts/warm.sh`) |
|---|---|---|
| 1 | หยุด 15 นาที | ข้ามรอบถัดไปหนึ่งรอบ (6–18 ชม.) |
| 2 | หยุด 1 ชม. | **หยุดทั้งหมด** จนกว่าจะ restart |
| 3 | หยุด 6 ชม. | — |
| 4+ | หยุด 24 ชม. | — |

ฝั่ง warm ยิงพลาดได้มากสุด **2 ครั้งก็หยุดเอง** ไม่มียิงซ้ำเรื่อย ๆ อีกแล้ว

สำเร็จเมื่อไหร่ตัวนับกลับเป็นศูนย์ · ทั้งคู่เริ่มนับใหม่เมื่อ restart (ตั้งใจ — รีสตาร์ตมักแปลว่า
คนเพิ่งแก้อะไร เช่นเพิ่งได้ไอพีที่โฮสต์ไม่ได้แบน) · purge เก็บสถานะในหน่วยความจำของโปรเซส `web`
ส่วน warm เก็บเป็นไฟล์ใน `/tmp` ของคอนเทนเนอร์ ซึ่งถูกล้างตอนคอนเทนเนอร์สตาร์ต

**ทำไมต้องมี** — ไฟร์วอลล์โฮสต์นับคำขอที่ล้มเหลวแล้วเลื่อนจากแบนชั่วคราวเป็นถาวร
พอโดนแบนแล้วยิงต่อไม่ได้อะไรกลับมาเลย แต่ยังเติมตัวนับให้มันอยู่ดี

⚠️ **`mirror-warm` ยิงหาโฮสต์แค่คำขอเดียวต่อรอบ ไม่ใช่หลายร้อย** — `warm.php` รันอยู่
บนโฮสต์ แล้ว**โฮสต์**เป็นฝ่ายไล่ดึงหน้าเว็บ 27 หน้ากับไฟล์อีก ~470 รายการจากเครื่องนี้เอง
(ทิศทาง โฮสต์ → เรา ซึ่งวิ่งผ่าน Cloudflare ไม่โดนแบน) · ที่ต้องมีตัวตัดวงจรคือมันยิงเองไม่มี
วันหยุด ถ้าไม่หยุดเองจะสะสมคำขอที่ล้มเหลวไปเรื่อย ๆ (ของเดิมยิงชั่วโมงละครั้ง = วันละ 24)

ยอมแลกอะไร: ช่วงที่ตัดวงจรอยู่ กดบันทึกแล้วสำเนาไม่ถูกล้างทันที ต้องรอหมดอายุเอง
≤ 2 นาที — แลกกับการไม่โดนแบนถาวรคุ้มกว่ามาก

**ปิดสนิทชั่วคราวได้ที่ `.env`** — คอมเมนต์ `MIRROR_PURGE_TOKEN` ทิ้ง ทั้งสองทางเช็ค
ตัวแปรนี้ตัวเดียวกัน ปิดตัวเดียวจบ (ทำไว้แล้ว 21 ส.ค. 2026 ตอนไอพีโดนแบน)

**กดบันทึกในหลังบ้านแล้วสำเนาบนโฮสต์ถูกล้างทันที** — `src/lib/mirrorPurge.ts` ยิงไปที่
`MIRROR_PURGE_URL` แบบไม่รอผล ไม่ตั้งค่าใน `.env` = ข้ามไปเงียบ ๆ

service `mirror-warm` อุ่นสำเนา **วันละ 2 ครั้ง — 09:30 กับ 15:30 เวลาไทย** จากเครื่องนี้
(ไม่ใช่ cron ฝั่งโฮสต์ เพราะการอุ่นต้องดึงของจากเครื่องนี้ เครื่องนี้เท่านั้นที่รู้ว่าตัวเองเปิดอยู่)
ทำให้ก่อนปิดเครื่องเย็นวันศุกร์สำเนาใหม่เสมอ สมาชิกเปิดเว็บได้ครบทุกหน้าตลอดเสาร์อาทิตย์

ใช้ busybox `crond` จริงในคอนเทนเนอร์ ตัวสคริปต์อยู่ที่ `scripts/warm.sh` (mount เข้าไปที่
`/warm.sh`) · เปลี่ยนเวลาได้ที่ `WARM_CRON` ใน `docker-compose.yml` แล้ว
`docker compose up -d mirror-warm` — แตะ service เดียว เว็บจริงไม่สะดุด

⚠️ **`TZ: Asia/Bangkok` ต้องอยู่คู่กับ `apk add tzdata` เสมอ** ไม่มี tzdata คอนเทนเนอร์
ยังเป็น UTC เงียบ ๆ แล้ว 09:30 จะกลายเป็น 16:30 เวลาไทย · เช็คด้วย
`docker compose exec mirror-warm date` ต้องขึ้น `+07`

⚠️ **busybox crond ไม่ส่ง env ของคอนเทนเนอร์ต่อให้งานที่มันสั่ง** ตอนสตาร์ตจึงเขียน
`MIRROR_PURGE_TOKEN` / `MIRROR_WARM_URL` / `TZ` ลง `/tmp/warm.env` ให้สคริปต์ source เอง
— เพิ่มตัวแปรใหม่ที่สคริปต์ต้องใช้ ต้องเพิ่มใน `grep -E` ของ entrypoint ด้วย
(กรองไว้ตั้งใจ ไม่เทรหัส FTP/LINE/OpenRouter ลงไฟล์)

## ฐานข้อมูลมีสองก้อน อย่าสลับกัน

| service | พอร์ตบนเครื่อง | ใครใช้ |
|---|---|---|
| `db` | `127.0.0.1:5435` | เว็บจริงใน Docker (ต่อผ่านชื่อ `db:5432` ในเน็ตเวิร์ก) |
| `db-dev` | `127.0.0.1:5436` | `npm run dev` และ prisma CLI บนเครื่อง |

`DATABASE_URL` ใน `.env` ชี้ **5436** ไว้แล้ว — `npx prisma migrate dev` จึงลงที่ db-dev เท่านั้น
ส่วน container เว็บจริงไม่เคยอ่านค่านี้ เพราะ `docker-compose.yml` ตั้ง `DATABASE_URL` ใน
`environment:` ซึ่งทับ `env_file` อยู่แล้ว

ถ้าจำเป็นต้องต่อฐานจริง (แก้ข้อมูลด่วน) ให้เปลี่ยน `.env` เป็น 5435 ชั่วคราวแล้ว**เปลี่ยนกลับทันที**

## รอบการ deploy

```bash
npm run build                       # ให้ผ่านก่อน แล้วค่อยแตะ container
docker compose up -d --build web    # เว็บจริงสะดุด ~1-2 นาที
```

แก้ schema ต้อง commit โฟลเดอร์ `prisma/migrations/` ไปด้วย — service `migrate` จะรัน
`prisma migrate deploy` ให้เองก่อน `web` สตาร์ต ถ้าลืม commit ตารางจริงจะไม่ตรงกับโค้ด

## อะไรที่เลื่อนเองได้ ต้องล็อกขนาดไว้ก่อนเสมอ

หน้าแรกมีของที่เปลี่ยนเองอยู่หลายจุด ทุกจุด**ต้องล็อกทั้งความสูงและความกว้างคอลัมน์**
ไม่งั้นพอเปลี่ยนรอบแล้วหน้าเว็บจะขยับ ซึ่งเห็นชัดมากเวลามันวนกลับมาหน้าที่มีของไม่ครบ

| ที่ไหน | ล็อกยังไง |
|---|---|
| แบนเนอร์สไลด์ | `aspect-[16/10]` ตรึงกรอบ · หัวข้อ `line-clamp-3` · คำอธิบาย `line-clamp-4` |
| ตารางดอกเบี้ย | กริด `repeat(perPage, ROW_H)` — หน้าที่มีไม่ครบเหลือช่องว่างไว้ · คอลัมน์ตัวเลข `w-24` |
| รายการประกาศ | กริด `repeat(PER_PAGE, minmax(4.25rem, auto))` |
| การ์ดกรรมการ | กรอบรูปขนาดคงที่ · ชื่อ/ตำแหน่ง `line-clamp-1` + `min-h` กันบรรทัดหายตอนไม่มีตำแหน่ง |
| ข่าววิ่ง | ทำเนื้อหาสองชุดกว้าง**เท่ากันเป๊ะ** แล้วเลื่อน `-50%` |

### จังหวะและท่าเปลี่ยนของสไลด์ทั้ง 4 อยู่ที่ `src/lib/slideMotion.ts`

หน้าแรกมีของที่เลื่อนเอง **4 อย่าง** — แบนเนอร์ · ตารางดอกเบี้ย · รายการประกาศ · ทำเนียบกรรมการ
**ตั้งใจให้คนละจังหวะกัน** ถ้าเปลี่ยนพร้อมกันทั้งหน้าจอจะกระพริบทีเดียวหมด ดูวุ่นวายมาก

| อะไร | ทุกกี่วินาที |
|---|---|
| รายการประกาศ | 9.1 (ช้าสุด — ต้องอ่าน 5 บรรทัด) |
| แบนเนอร์สไลด์ | 6.7 |
| ตารางดอกเบี้ย | 5.2 (เจ้าหน้าที่ตั้งเองได้ที่ `autoSeconds` ค่านี้เป็นแค่ค่าตั้งต้น) |
| ทำเนียบกรรมการ | 4.3 (เร็วสุด — ชื่อเดียวรูปเดียว) |

เลขพวกนี้จงใจไม่ลงตัวกันเอง (ไม่ใช่ 4000/6000/8000) สองอันจึงแทบไม่มีวันเปลี่ยนพร้อมกันซ้ำ ๆ
· ทุกอันมี **หลอดนับถอยหลัง** (`SlideProgress`) เต็มหลอดเมื่อไหร่ก็เปลี่ยนพอดี
ทำด้วย CSS animation ล้วน เอาเมาส์ชี้แล้วหยุดค้างตรงนั้นแล้วเดินต่อเอง ไม่ต้องคำนวณเวลาที่เหลือ

### ⚠️ สไลด์ทั้ง 4 ไม่สนใจ `prefers-reduced-motion` — ตั้งใจ อย่าเอากลับ

**21 ส.ค. 2026 เคยทำให้เครื่องที่ตั้งค่านั้นหยุดเลื่อนเองและซ่อนหลอด แล้วต้องถอยกลับ**

เหตุผล: เครื่องในสำนักงานที่เปิด **โหมดประหยัดพลังงาน** ของ Windows จะถูกปิดแอนิเมชัน
ให้เองโดยเจ้าตัวไม่รู้ตัว เจ้าหน้าที่เลยเห็นหน้าแรกนิ่งสนิทไม่มีหลอดนับ **แล้วนึกว่าเว็บพัง**
· เจ้าของเว็บสั่งให้แสดงเหมือนกันทุกเครื่อง โดยรับทราบว่าแลกกับอะไร

เช็คค่าจริงของเครื่องได้ด้วย PowerShell (อย่าอ่านบิตจากรีจิสทรี `UserPreferencesMask`
ผลไม่ตรงกับที่เบราว์เซอร์เห็น — เคยอ่านผิดมาแล้ว) เรียก API ตัวที่ Chrome ใช้จริง:

```powershell
# SPI_GETCLIENTAREAANIMATION = 0x1042 · false = เบราว์เซอร์รายงาน reduce
[Spi]::SystemParametersInfo(0x1042, 0, [ref]$val, 0)
```

ที่ยัง**เคารพ**ค่านี้อยู่ (อย่าไปแตะ): ป้าย "New" ที่ข่าววิ่ง · หน้าเปิดเว็บ (splash) ·
`Reveal` ตอนเลื่อน · หนังสือพลิกหน้า — ดู `@media (prefers-reduced-motion: reduce)` ใน `globals.css`

⚠️ **ห้ามใช้ `mode="wait"` กับ `AnimatePresence` ในงานสไลด์** — มันรอให้ของเก่าจางหายจนหมด
ก่อนแล้วค่อยเอาของใหม่ขึ้น สายตาจึงเห็นเป็น **วูบว่างหนึ่งจังหวะ = กระตุก**
(เคยเป็นแบบนั้นทั้งดอกเบี้ย ประกาศ และกรรมการ จนถูกทักเมื่อ 21 ส.ค. 2026)

ให้ใช้ `fadeSwap()` + `STACKED` จาก `slideMotion.ts` แทน — วางของเก่ากับของใหม่ไว้ใน
**ช่องกริดเดียวกัน** (`gridArea: "1 / 1"`) ทั้งคู่จึงซ้อนทับกันตอนจางสลับ
โดยความสูงยังเท่าตัวที่สูงสุดเสมอ ไม่มีวูบว่างและไม่มีการขยับ

⚠️ **กรอบที่ครอบ `AnimatePresence` ต้องถือ `flex-1` ไว้เอง** ห้ามย้ายไปไว้ที่ตัวที่จางหาย
ไม่งั้นจะมีเสี้ยววินาทีที่ที่ว่างหุบ แล้วปุ่มข้างล่างกระพริบขึ้นลงทุกรอบ

### ⭐ ของที่เลื่อนเองทุกตัวต้องผ่าน `src/lib/useAutoRotate.ts` เท่านั้น

**เพิ่มสไลด์/ของที่เลื่อนเองใหม่เมื่อไหร่ ห้ามเขียน `setInterval` เองในคอมโพเนนต์**
ให้เรียกตัวกลางตัวนี้ — เจ้าของเว็บสั่งไว้ 21 ส.ค. 2026 ว่าอะไรที่เป็น automation
ของสไลด์ต้องทำงานใต้ตัวเดียวกันหมด จะได้แก้ที่เดียวแล้วเปลี่ยนพร้อมกันทุกตัว

ตอนนี้ใช้กันครบทั้ง 4: แบนเนอร์ · ตารางดอกเบี้ย (`Hero.tsx`) · รายการประกาศ ·
ทำเนียบกรรมการ (`NewsSection.tsx`) · ตัวกลางให้มาครบทั้ง **หยุดรอจนกว่าจะเลื่อนมาเห็น** ·
**หยุดตอนเอาเมาส์ชี้** · **กดปุ่มเองแล้วรีเซ็ตนาฬิกา** · และคืน `paused` ให้หลอด
นับถอยหลังหยุดค้างพร้อมกัน

**ยังเลื่อนมาไม่ถึง = หยุดรอไว้ก่อน ไม่เล่น** เพราะถ้าปล่อยให้วิ่งตั้งแต่โหลดหน้าเสร็จ
พอสมาชิกเลื่อนลงมาถึงก็ผ่านไปหลายหน้าแล้ว จะเจอเป็นหน้าที่ 3 บ้าง 4 บ้างแบบสุ่ม
ไม่ได้เริ่มจากหน้าแรกอย่างที่ควร

**เลื่อนพ้นไปแล้วก็หยุดอีก** — `IntersectionObserver` แจ้งทั้งขาเข้าและขาออก พอออกจากจอ
`paused` กลับเป็น true แล้ว `clearInterval` ก็เก็บนาฬิกาทิ้ง ไม่มีอะไรวิ่งค้างอยู่นอกจอ
ลดภาระเครื่องของสมาชิกด้วย โดยเฉพาะมือถือเครื่องเก่า

⚠️ **ข่าววิ่ง (`NewsTicker`) เป็นข้อยกเว้นตัวเดียว — ปล่อยให้วิ่งตลอดตั้งใจ**
เจ้าของเว็บสั่งไว้ 21 ส.ค. 2026 · มันเป็น CSS `animation: ticker … infinite` ล้วน
ไม่มี `setInterval` และไม่ต้องเอาไปผูกกับ `useAutoRotate` **อย่าไป "ปรับให้ประหยัด"
ด้วยการหยุดมันตอนพ้นจอ** ข่าววิ่งที่สะดุดตอนเลื่อนกลับมาดูแย่กว่าที่ประหยัดได้

⚠️ **ใช้ `rootMargin` ติดลบ ห้ามใช้ `threshold`** — เคยใช้ `threshold: 0.35` แล้ว
**ไม่ทำงานจริง** (เจ้าของเว็บทักเอง 21 ส.ค. 2026) เพราะ `threshold` แปลว่า
"ต้องเห็นการ์ดกี่ % ของตัวการ์ดเอง" ซึ่งขึ้นกับความสูงการ์ดเทียบกับจอ —
การ์ดสูงกว่าจอเมื่อไหร่ (จอเตี้ย · ซูมหน้าเว็บ · กดปุ่มขยายตัวอักษร) อัตราส่วนจะ
ไม่มีวันแตะ 35% แล้วไม่เริ่มเล่นเลยทั้งที่เห็นเต็มจอ · `rootMargin: "-12% 0px -12% 0px"`
วัดจาก**จอ** ไม่ได้วัดจากการ์ด จึงใช้ได้ทุกขนาดเสมอ

⚠️ **ห้ามให้ hook คืน `ref` ออกมาในก้อนเดียวกับค่าอื่น** — กฎ `react-hooks/refs` จะฟ้อง
`Cannot access refs during render` ทุกจุดที่หยิบค่าจากก้อนนั้น (ไม่ใช่แค่ตัว ref)
ให้ **ส่ง ref เข้าไปเป็นพารามิเตอร์** แทน · และ **ห้าม `setState` ตรง ๆ ใน `useEffect`**
(กฎ `react-hooks/set-state-in-effect`) · ทั้งสองกฎนี้ **`npm run build` ไม่ฟ้อง**
ต้องรัน `npx eslint src/` ถึงจะเห็น — แก้ไฟล์พวกนี้แล้วต้องรันเองทุกครั้ง

## ตัวนำทางลอยด้านซ้ายของหน้าแรก + แถบบนที่หุบเก็บ

`src/components/home/SectionNav.tsx` — จุดกลมเรียงแนวตั้งลอยกลางจอฝั่งซ้าย
บอกว่ากำลังอ่านส่วนไหน กดกระโดดไปส่วนอื่นได้ · ปุ่ม ▲▼ เลื่อนทีละส่วน

- **รายการมาจาก `page.tsx` ไม่ได้คำนวณเอง** — ส่งลำดับที่กรองแล้ว (เปิดอยู่ + เรียงตาม
  ที่จัดในหลังบ้าน) เข้าไป จะได้ตรงกับที่วาดจริงเสมอ
- ชื่อสั้นอยู่ที่ `SECTION_SHORT` (`src/lib/homeSections.ts`) — **เพิ่มส่วนใหม่ในหน้าแรก
  ต้องเพิ่มที่นี่ด้วย** ไม่งั้นส่วนนั้นจะไม่มีในตัวนำทาง (เงียบ ๆ ไม่มีอะไรฟ้อง)
- `ticker` ไม่มีใน `SECTION_SHORT` ตั้งใจ — ข่าววิ่งเป็นแถบบางกว่าหนึ่งบรรทัด
  เลื่อนไปหยุดตรงนั้นแล้วเหมือนหน้าเว็บค้าง
- แต่ละส่วนถูกห่อด้วย `<div id="sec-<key>" class="scroll-mt-20">` ใน `page.tsx`
  — `scroll-mt-20` คือที่เว้นให้แถบเมนูที่ปักหัวจอไม่บังหัวข้อ
- **ไม่ดักล้อเมาส์** ให้เลื่อนทีละส่วน — เจ้าของเว็บเลือกทางนี้เอง คนที่เคยชินกับการ
  เลื่อนเองจะหงุดหงิด และ Ctrl+F จะพาไปไม่ถูกที่ · ให้กดปุ่มเอาแทน

**แถบบนของหัวเว็บ** (โลโก้ + ชื่อสหกรณ์ + นาฬิกา + ปรับขนาดตัวอักษร) โชว์เฉพาะตอนอยู่บนสุด
พอเลื่อนลงก็หุบเก็บ เหลือแต่แถวเมนู ได้พื้นที่อ่านคืนเกือบ 50px (`HeaderClient.tsx`)

⚠️ หุบด้วยท่ากริด `grid-rows-[1fr]` ↔ `grid-rows-[0fr]` **ไม่ใช่เดา `max-height`**
เพราะมันหุบพอดีความสูงจริงเสมอ ไม่ว่าชื่อสหกรณ์จะตกบรรทัดหรือไม่
· `overflow-hidden` ที่ลูกไม่ใช่ของแถม — **มันคือตัวที่ทำให้ช่องกริดยอมหดเหลือศูนย์ได้**
(ตรงนี้ `0fr` ตั้งใจให้หดจนหมด คนละเรื่องกับกฎ `minmax(0,…)` ของกริดคอลัมน์ข้างบน
ซึ่งกันไม่ให้ช่อง**กว้าง**ตามเนื้อหา)

**บทเรียนที่เจอจริง (20 ส.ค. 2026)**

1. ตารางดอกเบี้ยเคยใช้ `minHeight: perPage * 46` ซึ่งเดาความสูงแถวผิด (จริง 48px)
   หน้าที่เต็มจึงสูงกว่าหน้าที่ไม่เต็ม 14px แล้วการ์ดกระตุกทุกครั้งที่วนกลับ
   — **อย่าเดาความสูงแถวเป็นตัวเลข ให้ตรึงด้วยกริดไปเลย**
2. คอลัมน์ตัวเลขไม่ได้ล็อกกว้าง หน้าไหนมีเลข 2 หลักคอลัมน์กว้างขึ้น
   ชื่อรายการฝั่งซ้ายเลยขยับตามทุกครั้งที่เปลี่ยนหน้า
3. ข่าววิ่งเคยห่อแค่ครึ่งหลังด้วย `<span>` ครึ่งแรกจึงขาดช่องไฟท้ายไปหนึ่งช่อง
   สองครึ่งไม่เท่ากัน พอเลื่อน `-50%` เลยเพี้ยนไปครึ่งช่องไฟ **กระตุกทุกรอบ**

### ⚠️ `truncate` / `line-clamp` ไม่ทำงานถ้าลืมปลดล็อกความกว้าง — เจอเฉพาะภาษาไทย

**21 ส.ค. 2026 หัวข้อประกาศยาว ๆ ทะลุออกนอกขอบการ์ดบนหน้าแรก ทั้งที่ใส่ `truncate` ไว้แล้ว**

รายการประกาศเป็น `<ul class="grid">` แล้ว `<li>` เป็นช่องกริด · ช่องกริดแบบ `auto`
มีความกว้าง**ขั้นต่ำ**เท่ากับ `min-content` ของเนื้อหา — และ **ภาษาไทยไม่มีช่องว่าง
ระหว่างคำ ทั้งประโยคจึงนับเป็นคำเดียว `min-content` เลยยาวเท่าข้อความทั้งบรรทัด**

ผลคือช่องกริดกว้างตามข้อความ กล่อง `<p class="truncate">` เลยกว้างพอดีกับข้อความ
**ไม่มีอะไรล้นออกจากกล่องตัวเอง `text-overflow: ellipsis` จึงไม่ทำงาน** ข้อความวิ่งทะลุ
ขอบการ์ดออกไปเฉย ๆ โดยไม่มี `…` · ใส่ `min-w-0` ที่ตัวข้อความอย่างเดียว **ไม่พอ**
เพราะปัญหาอยู่ที่ช่องกริดชั้นนอก ไม่ใช่ตัวข้อความ

```
❌ <ul class="grid">              <li>            → ช่องกว้างตามข้อความ
✅ <ul class="grid grid-cols-1">  <li class="min-w-0">
```

`grid-cols-1` ของ Tailwind แปลเป็น `repeat(1, minmax(0, 1fr))` — เลข `0` ตัวนั้นแหละ
คือตัวบอกว่า "ช่องนี้แคบกว่าเนื้อหาได้"

### ⚠️ กับดักเดียวกันที่ "คอลัมน์" — ทำให้หน้าแรกกระตุกทุกครั้งที่สไลด์วน

**21 ส.ค. 2026 หน้าแรกกระตุกตอนของที่เลื่อนเองวนไปหน้าถัดไป** สาเหตุเดียวกันเป๊ะ
แต่ไปเกิดที่**คอลัมน์**แทนแถว

`grid-cols-[1.9fr_1fr]` ที่เขียนเป็นค่าอิสระ **ช่อง `fr` มีความกว้างขั้นต่ำเป็น `auto`
คือ `min-content` ของเนื้อหา** → ชื่อรายการดอกเบี้ยที่ยาวที่สุด**ของหน้านั้น**ดันคอลัมน์ขวา
ให้กว้างขึ้น → พอเลื่อนไปหน้าถัดไปความยาวเปลี่ยน คอลัมน์ก็เปลี่ยนตาม → **แบนเนอร์ฝั่งซ้าย
ขยับทุกครั้งที่วน** · ในสไลด์แบนเนอร์เองก็เป็น หัวข้อแต่ละใบยาวไม่เท่ากัน รูปฝั่งขวาเลยขยับตาม

```
❌ grid-cols-[1.9fr_1fr]
✅ grid-cols-[minmax(0,1.9fr)_minmax(0,1fr)]
```

⚠️ **`grid-cols-2` `grid-cols-3` แบบมีเลขไม่มีปัญหา** เพราะ Tailwind ใส่ `minmax(0,1fr)`
ให้อยู่แล้ว — **ที่อันตรายคือค่าอิสระในวงเล็บเหลี่ยมที่เขียน `fr` ตรง ๆ**
ตรวจทั้งโปรเจกต์ด้วย `grep -rn "grid-cols-\[" src/` แล้วต้องไม่เจอ `fr` ที่ไม่ได้ห่อ `minmax(0,…)`

ที่แก้ไปแล้ว: `Hero.tsx` (กริดนอก + ในสไลด์ + ตารางดอกเบี้ย) · `NewsSection.tsx` ·
`CoopCalendar.tsx` · `MemberCorner.tsx`

**กฎ:** ทุกที่ที่ใส่ `truncate` หรือ `line-clamp` ต้องไล่ดูให้ครบว่า**บรรพบุรุษทุกชั้น
ที่เป็นช่องกริดหรือลูกของ flex ยอมแคบกว่าเนื้อหา** (`min-w-0` / `minmax(0,…)`)
· และใส่ `overflow-hidden` ที่การ์ดชั้นนอกไว้เป็นด่านสุดท้ายเสมอ
พลาดตรงไหนก็ยังไม่ทะลุขอบการ์ดออกไป

## ห้ามตั้งชื่อไฟล์รูปเป็นภาษาไทย

`src/data/asset/**` ถูก import เข้า bundle ตรง ๆ Next เอาชื่อไฟล์ไปใส่ header `Link` (early preload)
ซึ่งเก็บได้แค่ Latin-1 พอมีอักษรไทยจะ throw ทุก request:

```
TypeError: Cannot convert argument to a ByteString ... value of 3626
```

ชื่อไฟล์เป็น ASCII เสมอ ส่วนชื่อคน/หัวข้อภาษาไทยเก็บในโค้ด เช่น array `members` ใน
`src/app/about/directory/board/page.tsx` ที่จับคู่ `committee-01.png` → ชื่อกรรมการ

**กฎเดียวกันใช้กับค่าใน HTTP header ทุกที่** — เคยพลาดมาแล้วที่ `X-Title` ตอนเรียก OpenRouter
ใส่ชื่อระบบเป็นภาษาไทย แล้ว `fetch` throw ทุกครั้งที่กดให้ AI อ่านภาพ

## AI อ่านภาพประกาศ — เรียกผ่าน OpenRouter

`src/lib/ai.ts` ยิง HTTP ตรงไปที่ `openrouter.ai/api/v1/chat/completions` (หน้าตาแบบ OpenAI)
ไม่ได้ใช้ SDK ของเจ้าไหน คีย์อ่านจาก `OPENROUTER_API_KEY` · รุ่นเปลี่ยนได้ที่ `AI_MODEL`
ไม่ตั้ง = `anthropic/claude-sonnet-5`

รุ่นที่เลือกต้อง **อ่านภาพได้ + รองรับ structured outputs** เช็คได้ที่
`GET /api/v1/models` ดูว่ามี `image` ใน `architecture.input_modalities` และ
`structured_outputs` ใน `supported_parameters` ไม่งั้นจะได้ JSON โครงไม่ตรงแล้ว parse พัง

ไม่มีคีย์ = `AI_READY` เป็น false ปุ่ม AI ถูกซ่อนไปเอง หลังบ้านยังใช้ได้ปกติแค่ต้องพิมพ์เอง
**AI ไม่เคยบันทึกลงฐานเอง** — คืนค่าไปเติมในฟอร์มให้เจ้าหน้าที่ตรวจแล้วกดยืนยันเสมอ

### อัปสไลด์แล้ว AI อ่านให้ — รวมถึงช่วงเวลาเผยแพร่

อัปรูปในหน้า `/admin/home/slides` แล้ว AI อ่านภาพให้เลย ได้ **หัวข้อ · คำอธิบาย ·
วันเริ่มแสดง · วันสิ้นสุด** แล้วหน้าเว็บจริงกรองตามช่วงวันนั้นเอง
(`getSlides()` ใน `src/lib/content.ts`) — ประกาศเก่าหายจากหน้าแรกเองไม่ต้องมาคอยลบ

⚠️ **`startsAt` / `endsAt` ว่างไม่ใช่อาการเสีย** — prompt สั่งไว้ชัดว่า
**ห้ามเดาวันที่ที่ไม่ได้เขียนอยู่ในภาพ** โปสเตอร์ที่ไม่มีวันหมดเขต (เช่นใบแนะนำวิธีใช้บริการ)
จึงได้ `""` กลับมาถูกต้องแล้ว · เจ้าหน้าที่ตั้งเองได้ที่ช่องใต้สไลด์

### คิวสไลด์จัดให้เอง — ตัวที่ใกล้หมดเขตขึ้นก่อนเสมอ

`src/lib/slideQueue.ts` — เจ้าของเว็บสั่งไว้ 25 ส.ค. 2026 ให้แบ่งเป็น **2 กลุ่ม**

| ลำดับ | กลุ่ม | เรียงยังไงในกลุ่ม |
|---|---|---|
| 1 | มีวันหยุดเผยแพร่ (`endsAt`) | วันที่ใกล้ถึงก่อนขึ้นก่อน |
| 2 | ไม่มีวันหยุดเผยแพร่ | ตามลำดับที่เจ้าหน้าที่จัดไว้เอง |

เหตุผล: ประกาศที่ใกล้หมดเขตคือประกาศที่ต้องรีบอ่าน (สมัครทุน · ยื่นเอกสาร · เลือกตั้ง)
ถ้าไปอยู่ท้ายคิว กว่าจะวนมาถึงก็เลยกำหนดแล้ว

**จัดคิวให้เองตอนไหน** — ตอนเพิ่มสไลด์ใหม่ และตอนที่ `startsAt`/`endsAt` ถูกบันทึก
(`requeueSlides()` ใน `src/app/api/admin/slides/route.ts`) · **ไม่จัดตอนกดปุ่มขึ้น/ลง**
ไม่งั้นเจ้าหน้าที่จะขยับลำดับเองไม่ได้เลย โดนรีเซ็ตทุกครั้งที่พิมพ์อะไรก็ตาม

ลำดับเพี้ยนเมื่อไหร่ (เพิ่งกดขึ้น/ลงเอง หรือข้อมูลเก่าที่มีมาก่อน) หน้าหลังบ้านจะโชว์
ปุ่มเหลือง "ลำดับตอนนี้ยังไม่ตรงกับคิว" ให้กดจัดใหม่ — กดแล้วยิง `PUT /api/admin/slides/`
ด้วยลำดับที่คำนวณจากกฎเดียวกัน

- ⚠️ **เรียงแบบนิ่ง (stable)** ของที่วันเท่ากันหรือไม่มีวันเลย ต้องคงลำดับเดิมเสมอ
  ไม่งั้นทุกครั้งที่บันทึกอะไรสักอย่าง สไลด์จะสลับที่กันเองจนเจ้าหน้าที่งง
- ⚠️ วันที่ในฐานเก็บเป็น "เที่ยงคืนเวลาไทย" = 17:00Z ของวันก่อนหน้า **ต้องบวก 7 ชม.
  ก่อนตัดเอา YYYY-MM-DD** ไม่งั้นได้วันก่อนหน้าไปหนึ่งวัน (เรื่องเดียวกับ `day::date` ในหัวข้อสถิติ)
- สไลด์ที่หมดอายุแล้วจะลอยขึ้นบนสุดของหลังบ้าน (วันเก่าสุด) — ตั้งใจ ให้เห็นว่ามีของค้างรอลบ
  · บนหน้าเว็บจริงมันถูกกรองออกอยู่แล้วโดย `getSlides()` ไม่ได้ไปแย่งที่ใคร

### ⚠️ ห้ามให้เซิร์ฟเวอร์วนกลับไปขอไฟล์ของตัวเองผ่าน HTTP

**21 ส.ค. 2026 AI อ่านภาพสไลด์ไม่ทำงานเลย** เพราะ `POST /api/admin/ai/read-image`
ดึงไฟล์ที่อัปไว้ด้วยการวนกลับไปขอตัวเอง โดยประกอบ URL จาก**หัว `Host` ของคำขอ**
(`new URL(url, request.url)`) ซึ่งต่อไม่ได้จริงในคอนเทนเนอร์

```
http://127.0.0.1:3000/uploads/…   200 OK
http://localhost:3000/uploads/…   ต่อไม่ได้   (ในคอนเทนเนอร์ localhost ไปทาง ::1)
http://localhost:8030/uploads/…   ต่อไม่ได้   (พอร์ตนี้อยู่บนเครื่อง ไม่ได้อยู่ในคอนเทนเนอร์)
```

ตอบ 502 ทุกครั้ง → AI ไม่เคยได้อ่านภาพ → หัวข้อกลายเป็นชื่อไฟล์ ช่องวันที่ว่าง
**ดูเหมือนความสามารถหายไป ทั้งที่ทุกชิ้นส่วนทำงานได้หมด**

**ตอนนี้อ่านจากดิสก์ตรง ๆ** (`/app/public/uploads/…` — `process.cwd()` ในคอนเทนเนอร์คือ `/app`)
ไม่ต้องพึ่งว่าคำขอเข้ามาทางโดเมนไหน · ไฟล์บนโดเมนอื่น (พื้นที่ FTP) ยังดึงผ่าน HTTP ตามเดิม

**บทเรียน:** ทดสอบชิ้นส่วนทีละอันผ่านหมด **ไม่ได้แปลว่าทั้งเส้นทางทำงาน** —
คราวนั้นตรวจคีย์ · รุ่น · schema · การอ่านรูป 600px ผ่านหมดทุกอัน แล้วสรุปว่า
"ทำงานครบทุกจุด" ทั้งที่ยังไม่เคยทดสอบ**ข้อต่อ** คือขั้นตอนที่เซิร์ฟเวอร์ไปหยิบไฟล์

หน้าจอก็ไม่เคยแสดง error ตัวนี้ให้เห็น (ขึ้นว่า "AI เติมหัวข้อให้" ทุกกรณี)
ตอนนี้แยกข้อความเป็น 3 แบบแล้ว: ได้วันที่ · ในภาพไม่มีวันที่ · เรียกไม่สำเร็จพร้อมเหตุผล

**เวลาสงสัยว่า AI พัง ให้ทดสอบตรง ๆ ก่อนแก้โค้ด** — ยิงจากในคอนเทนเนอร์ `web`
ด้วยรูปจริงใน `/app/public/uploads/` ไปที่ OpenRouter พร้อม `response_format` ชุดเดียวกับ
ที่โค้ดใช้ · รูปที่เก็บถูกย่อเหลือ 600px แต่ทดสอบแล้ว **อ่านข้อความไทยออกครบทั้งใบ**
ขนาดไม่ใช่ปัญหา · **แล้วอย่าลืมทดสอบขั้นตอนหยิบไฟล์ด้วย ไม่ใช่แค่ตัว AI**

อ่านโปสเตอร์หนึ่งใบ ≈ 2,000 token ≈ 0.006 ดอลลาร์ ดูยอดคงเหลือได้ที่
`curl -H "Authorization: Bearer $OPENROUTER_API_KEY" https://openrouter.ai/api/v1/key`

## สถิติผู้เข้าชมนับเอง ไม่ได้ใช้บริการภายนอก

`src/lib/analytics.ts` + `POST /api/track` — หน้าเว็บยิงแจ้งด้วย JavaScript หลังโหลดเสร็จ
(ไม่นับฝั่ง server ตอน render เพราะบอทกับตัวไต่เว็บจะถูกนับไปด้วย ตัวเลขจะสูงเกินจริง)

**นับทุกโดเมนสาธารณะ** — เข้าทาง spsccoop.com หรือ spsccoop.org ก็คือคนมาอ่านเว็บสหกรณ์
เหมือนกัน ยอดรวมกัน (`ANALYTICS_HOST` ใส่หลายโดเมนคั่นด้วยจุลภาค)
· โดเมนใน `ADMIN_HOST` **ไม่ถูกนับเสมอ** ต้องกันไว้ตรง ๆ เพราะ admin.spsccoop.org
ลงท้ายด้วย `.spsccoop.org` ถ้าไม่กันจะถูกนับไปด้วย

(งานหลังบ้านไม่เคยเข้ายอดอยู่แล้ว — `shouldTrack` ตัด `/admin` ทิ้ง และตัวแจ้งนับ
`PageTracker` ติดอยู่แต่บนหน้าเว็บสาธารณะ)

คำขอจากสมาชิกเดินทางสองต่อ ตัวมิเรอร์จึงต้องบอกหลังบ้านว่าคนอ่านเปิดโดเมนไหน ไอพีอะไร
ผ่านหัว `x-public-host` กับ `x-visitor-ip` (ดู `php-frontend/index.php`)
**แก้ไฟล์นั้นแล้วต้องอัปขึ้นโฮสต์ด้วย** ไม่งั้นหลังบ้านจะเห็นแต่ไอพีของโฮสต์
แล้วยอด "จำนวนคน" ค้างที่ 1 คนต่อวัน — เคยเป็นมาแล้ว

### ตัวนับที่ท้ายเว็บ — นับ "ครั้ง" และนับต่อจากยอดของเว็บเดิม

**นับเป็นครั้ง ไม่ใช่คน** — คนเดิมเปิดเว็บสิบรอบก็นับสิบครั้ง (ตั้งใจให้เป็นแบบนั้น
เหมือนตัวนับของเว็บเดิม) เลขมาจากผลรวม `PageView.count` ทั้งหมด ไม่ใช่จำนวนแถวใน `VisitorDay`

จำนวน "คน" ยังเก็บอยู่ที่ `VisitorDay` เหมือนเดิม โชว์แยกไว้ในหน้าภาพรวม
(บรรทัดเล็กใต้ตัวเลขใหญ่) — **อย่าเอาสองหน่วยนี้มาปนกัน**

เว็บเดิมนับมาหลายปีก่อนย้ายมาระบบนี้ ถ้าเริ่มจากศูนย์ ตัวเลขจะร่วงจากสองแสนกว่า
เหลือหลักพัน เหมือนเว็บเพิ่งเปิด · ยอดที่เห็น = **ยอดยกมา + ที่ระบบนี้นับได้เอง**
(ตั้งไว้ 228,000 เมื่อ 20 ส.ค. 2026) แก้ได้ที่หลังบ้าน → ส่วนท้ายเว็บ → "ยอดเข้าชมยกมาจากเว็บเดิม"
เก็บใน `Setting["siteInfo"].visitorCarriedOver`

เลขนี้เดิมคือช่อง `memberCount` ("จำนวนสมาชิก") ที่พิมพ์เอง แต่ถูกเอาไปโชว์เป็นสถิติผู้เข้าชม
ที่ท้ายเว็บมาตลอด — ตอนนี้ตัวเลขที่โชว์มาจากระบบนับจริงแล้ว ไม่ใช่เลขที่พิมพ์ค้างไว้

หน้าภาพรวมในหลังบ้านโชว์ยอดเดียวกันกับท้ายเว็บ เจ้าหน้าที่จะได้ไม่งงว่าทำไมสองที่ไม่ตรงกัน
· สมาชิกส่วนใหญ่อ่านผ่านสำเนาบนโฮสต์ ตัวเลขที่เห็นจึงขยับตามรอบล้าง/อุ่นแคช ไม่ได้วิ่งสด ๆ

เก็บสองตาราง: `PageView` = จำนวนครั้งต่อหน้าต่อวัน · `VisitorDay` = จำนวนคนต่อวัน
**ไม่เก็บ IP ไม่เก็บ cookie** — ลายนิ้วมือถูกแฮชรวมกับวันที่ คนเดิมจึงกลายเป็นค่าใหม่ทุกวัน
ตั้ง `ANALYTICS_SALT` ใน `.env` เพื่อกันคนเดารหัสย้อนกลับ

วันที่เก็บเป็น "เที่ยงคืนเวลาไทย" ซึ่งใน Postgres คือ 17:00Z ของวันก่อนหน้า —
`day::date` ใน psql จะโชว์วันที่ก่อนหน้า 1 วัน **นั่นถูกแล้ว** อย่าไปแก้

กราฟวาดด้วย HTML/CSS ล้วนใน `src/components/admin/StatsCharts.tsx` ไม่มี chart library
ซีรีส์เดียว = สีเดียว (`#1c7fca`) ผ่านเกณฑ์ contrast บนพื้นขาวแล้ว

## แก้เนื้อหาหน้าเว็บมีสองโหมด — EditUI กับ EditCode

หน้าแก้ไข (`/admin/pages/<id>`) แบ่งเป็นส่วน ๆ ตามลำดับที่ต้องทำ: ชื่อ/ที่อยู่ → เนื้อหา →
ที่เก็บไฟล์ → AI → เผยแพร่ · ส่วนเนื้อหามีสามแท็บ

| แท็บ | ทำอะไร |
|---|---|
| **แก้บนหน้าเว็บ** (EditUI) | คลิกที่ข้อความแล้วพิมพ์ทับได้เลย — ค่าตั้งต้น |
| **แก้เป็นโค้ด** (EditCode) | พิมพ์ HTML ตรง ๆ |

ชื่อบนแท็บเป็นภาษาไทย ป้าย EditUI/EditCode ห้อยเป็นตัวเล็ก — เจ้าหน้าที่ที่ไม่ได้เรียน
คอมพิวเตอร์อ่านคำอังกฤษล้วนแล้วไม่รู้ว่าคืออะไร แต่ชื่อเดิมยังต้องอยู่เพราะทีมเรียกกันแบบนั้นแล้ว

⚠️ **คำที่ใช้ในหน้าจอต้องเป็นคำที่เจ้าหน้าที่พูดกันจริง ไม่ใช่ศัพท์คนทำเว็บ**
ชื่อชนิดเนื้อหาทั้งหมดอยู่ที่ `BLOCK_LABEL` และคำอธิบายอยู่ที่ `BLOCK_HINT`
(`src/lib/pageBlocks.ts`) — เพิ่มชนิดใหม่ต้องใส่ทั้งสองที่
· ห้ามใช้คำเดียวสองความหมายในจอเดียว: **"หัวข้อ" สงวนให้ heading เท่านั้น**
หัวตารางเรียก "ชื่อคอลัมน์" ชื่อบนปุ่มแท็บเรียก "ชื่อหัวเรื่อง"

ทั้งสองโหมดแก้ **เนื้อหาก้อนเดียวกัน** สลับไปมาได้ตลอด · โหมดที่เลือกจำไว้ใน cookie
`spsc_page_edit_mode` (เซิร์ฟเวอร์อ่านตอน render แท็บจึงไม่กระพริบ เหมือนสวิตช์ AI)

ไม่มีแท็บ "ดูตัวอย่าง" แล้ว — EditUI คือหน้าเว็บจริงอยู่แล้ว ไม่ต้องมีที่ให้ดูซ้ำอีกที่

### สองหน้าบันทึกคนละแบบ — ต้องบอกให้เห็น ไม่ใช่ให้จำเอา

| หน้า | บันทึกยังไง |
|---|---|
| หน้าเนื้อหา (`/admin/pages/<id>`) | **ต้องกดบันทึกเอง** เพราะตอนบันทึกมี AI จัดรูปแบบ + ซ่อมโครงสร้าง ซึ่งทำทุกครั้งที่พิมพ์ไม่ได้ |
| หน้าสไลด์ (`/admin/home/slides`) | **บันทึกให้เองทันที** ที่คลิกออกจากช่อง |

ความต่างนี้คือทางที่งานหายได้ง่ายที่สุด จึงกันไว้สามชั้น: บรรทัดบอกวิธีใช้บนหัวแท็บ ·
**แถบดำติดล่างจอเมื่อมีของค้างยังไม่บันทึก** · เตือนอีกครั้งตอนจะปิดแท็บ (`beforeunload`)
ส่วนหน้าสไลด์เขียนไว้ตรง ๆ ว่าบันทึกให้เอง จะได้ไม่นั่งหาปุ่มบันทึก

### EditUI ทำงานยังไง

`src/lib/pageBlocks.ts` อ่าน HTML เป็น "ก้อน" (หัวข้อ · ย่อหน้า · ตาราง · การ์ด PDF ·
แท็ปเมนู · ทำเนียบบุคลากร ฯลฯ) แล้วเขียนกลับเป็น HTML หน้าตาเดียวกับที่แถบเครื่องมือเขียน
**ก้อนที่อ่านไม่ออกเก็บทั้งดุ้นเป็นก้อน "โค้ด HTML"** ไม่มีทางที่เนื้อหาจะหายเพราะเปิดหน้าจอนี้

**หน้าจอวาดด้วย `.prose-page` ชุดเดียวกับหน้าเว็บจริง** ไม่ได้ทำ CSS เลียนแบบไว้ต่างหาก —
หัวข้อจึงใหญ่เท่าของจริง ตารางมีเส้นเหมือนของจริง การ์ด PDF หน้าตาเหมือนของจริง
ส่วนที่เพิ่มมาคือคลาส `.prose-edit` ซึ่งเป็นแค่เครื่องมือที่ลอยอยู่เหนือหน้าเว็บ
(กรอบตอนชี้ · ปุ่มเลื่อน/ลบ · เส้น ＋ แทรกก้อน · แถบตั้งค่าของก้อนที่เลือก)

⚠️ **แก้ CSS ของ `.prose-page` แล้วต้องดู EditUI ด้วย** — สองที่นี้ใช้กฎชุดเดียวกัน
และกฎที่อิง "ลูกโดยตรงของ `.prose-page`" (`> * + *`, `h2 + *`, `> p:first-child`)
ถูกเขียนซ้ำไว้ในส่วน `.prose-edit` เพราะตัวแก้ไขห่อทุกก้อนด้วย `<div data-block>` อีกชั้น
เพื่อวางปุ่ม ระยะห่างจึงจะเท่าหน้าจริงได้ก็ต่อเมื่อกฎสองชุดนี้ตรงกัน

ข้อความที่พิมพ์ทับได้คือแท็กจริง (`<h2 contentEditable>` ไม่ใช่ `<input>` ที่แต่งให้เหมือน)
— ดู `src/components/admin/RichText.tsx` · ค่าตั้งต้นถูกใส่ตั้งแต่ตอน render (ไม่รอ `useEffect`)
ไม่งั้นหน้าจอขึ้นมาเป็นช่องเปล่าแวบหนึ่งก่อนตัวหนังสือจะโผล่

### หน้าแบนเนอร์สไลด์ก็ใช้แนวเดียวกัน

`/admin/home/slides` วาดสไลด์เป็นกรอบ 16:10 ข้อความซ้าย-ภาพขวา แบบเดียวกับที่ขึ้นหน้าแรกจริง
คลิกที่หัวข้อ/คำอธิบายแล้วพิมพ์ทับได้

⚠️ **หน้าตาสไลด์อยู่สองที่ ต้องแก้พร้อมกันเสมอ** — `src/components/home/Hero.tsx`
(ของจริงที่สมาชิกเห็น) กับ `SlideCard` ใน `src/components/admin/SlidesManager.tsx`
(ที่เจ้าหน้าที่แก้) ถ้าแก้ที่เดียว หลังบ้านจะโชว์คนละหน้าตากับของจริงทันที
ตอนนี้ทั้งสองใช้: `rounded-3xl` · พื้นไล่สี `from-white via-sky-50 to-brand-50` ·
แสงนุ่มหลังภาพ (`blur-2xl`) · แถบสีนำหัวข้อ · หัวข้อ `text-brand-800`
ใช้คลาส `.edit-*` ชุดเดียวกับ EditUI — **แก้ CSS พวกนั้นแล้วกระทบทั้งสองหน้า**

หัวข้อสไลด์เก็บเป็น **ข้อความล้วน** ไม่ใช่ HTML (หน้าเว็บวาดด้วย `{slide.title}`)
จึงใช้ `RichText` โหมด `plain` — ถ้าปล่อยให้ใส่แท็กได้ แท็กจะไปโผล่เป็นตัวหนังสือบนหน้าเว็บ

อัปรูปแล้วได้สไลด์เลย ไม่ต้องกรอกฟอร์มก่อน (มีคีย์ AI ก็อ่านหัวข้อ/ช่วงวันที่จากภาพมาใส่ให้)
— ของเดิมแก้หัวข้อสไลด์ที่มีอยู่แล้วไม่ได้เลย ต้องลบทิ้งแล้วทำใหม่ ทั้งที่ API รองรับมาตลอด

### ทำเนียบบุคลากร: ซ่อนชื่อใต้รูปได้ เมื่อรูปมีชื่อพิมพ์ติดมาแล้ว

รูปบุคลากรที่ร้านทำมาให้มัก **มีแถบชื่อ-สกุลกับตำแหน่งพิมพ์ติดอยู่ในภาพเลย**
(ดู `/about/directory/staff/`) ถ้าหน้าเว็บพิมพ์ชื่อให้อีกใต้รูป ก็เห็นชื่อซ้ำสองรอบ
· เจ้าของเว็บขอให้ปิดได้ 25 ส.ค. 2026 — เลือกที่ก้อนทำเนียบ → "ชื่อกับตำแหน่งใต้รูป: ซ่อน"

**ซ่อนด้วย CSS ไม่ได้ลบข้อความทิ้ง** — `<figcaption>` ยังอยู่ในเนื้อหาครบ แค่ใส่คลาส
`no-caption` ที่กล่องนอก (`src/lib/pageBlocks.ts` → `noCaption`) กดเปิดกลับได้ทันที
ชื่อไม่หายไปไหน และ `alt` ของรูปยังเก็บชื่อไว้เสมอ คนใช้โปรแกรมอ่านหน้าจอจึงยังรู้ว่าเป็นใคร

- ⚠️ **`no-caption` ต้องอยู่ใน `ALLOWED_CLASSES`** (`src/lib/pageHtml.ts`) ลืมใส่ = ตัวกรอง
  กินคลาสทิ้งตอนกดบันทึก แล้วชื่อจะเด้งกลับมาเองโดยไม่มีอะไรฟ้อง
- ⚠️ กฎ CSS เขียนเป็น `.prose-page:not(.prose-edit)` — **ใน EditUI ห้ามซ่อน** ไม่งั้น
  เจ้าหน้าที่จะแก้ชื่อไม่ได้เลย · ในตัวแก้ไขทำให้จางลงเฉย ๆ พอให้รู้ว่าจะไม่ขึ้นจริง

⚠️ **แก้ค่าในฐานตรง ๆ ตอนมีคนเปิดหน้าแก้ไขค้างอยู่ = โดนทับ** — 25 ส.ค. 2026 ใส่คลาสให้
ทางฐาน แล้วโดนกดบันทึกจากแท็บที่เปิดค้างไว้ทับหายสองรอบ (แท็บนั้นยังใช้โค้ดชุดเก่าที่ยัง
ไม่รู้จัก `noCaption` มันจึงเขียนกลับโดยไม่มีคลาส) · deploy โค้ดใหม่แล้วต้องบอกให้กด F5
ที่หน้าแก้ไขก่อนบันทึกครั้งต่อไปเสมอ

### ⚠️ กล่องแท็บต้องเป็นของท้ายหน้าเสมอ — เคยเป็นบั๊กที่กลืนเนื้อหา

**22 ส.ค. 2026 หน้าสวัสดิการมีแท็บแล้วต่อด้วยหัวข้อ "ระเบียบ" กับ "เอกสาร"
ผลคือสองหัวข้อนั้นถูกดูดเข้าไปอยู่ในแท็บสุดท้ายทั้งก้อน** รายการในแท็บบวมจาก 7 เป็น 31

`fixTabsStructure()` ใน `src/lib/htmlStructure.ts` ไล่จากแท็บสุดท้ายไปจน `html.length`
เพราะคิดว่ากล่องแท็บเป็นของท้ายหน้าเสมอ — แก้แล้วให้หาตำแหน่ง `</div>` ที่ปิดกล่องจริง
ด้วยการนับชั้น แล้วซ่อมเฉพาะข้างในกล่อง ส่วนที่อยู่ถัดจากนั้นคงไว้ตามเดิม

⚠️ **ห้ามหา `</div>` ปิดด้วย regex ตัวแรกที่เจอ** — กล่องแท็บมี `<div>` ซ้อนข้างในหลายชั้น
ต้องนับชั้นเสมอ (มีฟังก์ชัน `closingDiv` ให้แล้วทั้งใน `htmlStructure.ts` และ `welfareGroups.ts`)

### ⚠️ อย่าเขียนไฟล์ `.ts` ที่มี regex ด้วย `python -c` / heredoc

**22 ส.ค. 2026 เสียเวลาไล่บั๊กนานมากเพราะเรื่องนี้** — เขียน `\b` ลงไปในสตริงของ Python
แล้วมันกลายเป็น **อักขระ backspace ตัวจริง (0x08)** ในไฟล์ ไม่ใช่ตัวอักษรสองตัว `\` กับ `b`

ผลคือ regex `/<div\b|<\/div>/` กลายเป็น `/<div␈|<\/div>/` ซึ่ง **หา `<div` ไม่เจอเลย**
นับชั้นผิดตั้งแต่ตัวแรก · TypeScript ไม่ฟ้อง ESLint ไม่ฟ้อง build ผ่าน หน้าเว็บขึ้นปกติ
แค่ผลลัพธ์ผิดเงียบ ๆ · และมองไม่เห็นด้วยตาเพราะ backspace ไม่มีรูปร่าง

```bash
grep -n "const tag" ไฟล์.ts | cat -v     # ^H = backspace ปนอยู่
python -c "print(open('ไฟล์.ts','rb').read().count(bytes([8])))"
```

**ให้ใช้เครื่องมือแก้ไฟล์ตรง ๆ แทน** · ถ้าเลี่ยงไม่ได้ ต้องประกอบไบต์เอง
(`bytes([92,98])` = `\b`) และ**เช็คด้วย `cat -v` ทุกครั้งหลังเขียน**

⚠️ **แก้ `pageBlocks.ts` แล้วต้องรันตัวตรวจทุกครั้ง** — มันแปลงเนื้อหาจริงทุกหน้าไปกลับ
แล้วเทียบว่าตัวอักษรครบและจำนวนโครงเท่าเดิม พร้อมจำลอง "คนคลิกเข้าไปทุกช่อง" ด้วย

```bash
docker compose exec -T db psql -U coopsmile coopsmile -tAc   "select json_agg(json_build_object('slug', slug, 'body', body)) from \"Page\"" > pages.json
npm run check:blocks -- pages.json out.txt      # ผลออกเป็นไฟล์ เชลล์ทำไทยเพี้ยน
```

ตัวตรวจนี้จับบั๊กจริงมาแล้วสองตัวตอนสร้างฟีเจอร์: ไอคอน PDF ในตารางหน้าดาวน์โหลด
(`<a class="pdf-icon" style="--pdf-size:30px">`) หายเพราะตัวล้างข้อความตัด `class` ทิ้ง
แค่คลิกเข้าไปในช่องแล้วคลิกออก · และชื่อในทำเนียบที่ปรึกษาหายเพราะหน้านั้นเขียน
`<figure>` เปล่า ๆ ไม่มี `<span class="person-name">` อย่างหน้าอื่น

`cleanInline()` จึงล้างแบบ **"เก็บของที่รู้จัก"** ไม่ใช่ "ตัดทุกอย่างที่ไม่ใช่ข้อความ" —
เพิ่ม class ใหม่ให้เนื้อหาเมื่อไหร่ ต้องเพิ่มใน `ALLOWED_CLASSES` (`src/lib/pageHtml.ts`) ด้วย
ไม่งั้นมันจะหายตอนมีคนแก้ช่องนั้น

## ระเบียบ/แบบฟอร์มไปเกาะการ์ดของเรื่องที่มันใช้ — 3 หน้าใช้ตัวเดียวกัน

`src/lib/pageDocs.ts` — หน้า **สวัสดิการ** · **เงินให้กู้** · **เงินรับฝาก** โครงเหมือนกันหมด
คือข้างบนเป็นการ์ดของแต่ละเรื่อง ข้างล่างเป็นตารางเอกสารยาว ๆ ที่ไม่ได้บอกว่าฉบับไหน
ของเรื่องไหน สมาชิกต้องเดาเอง (เงินให้กู้มีระเบียบ 30 ฉบับ แบบฟอร์ม 18 ฉบับ)

ตอนนี้ตัวอ่านดึงตารางท้ายหน้าออกมา จับคู่กับชื่อเรื่องบนการ์ด แล้วเอาไปแปะใต้การ์ดนั้น
· เจ้าของเว็บสั่งไว้ 24 ส.ค. 2026 ให้ทำทั้งสามหน้าเหมือนกัน

| หน้า | การ์ดมาจาก | ตารางเอกสารที่ถูกดึง |
|---|---|---|
| `/welfare/` | `readWelfare()` (แท็บ+ตารางในเนื้อหา) | ระเบียบสวัสดิการ · เอกสารดาวน์โหลดสวัสดิการ |
| `/loans/` | `groupLoans()` (อัตราดอกเบี้ยจาก Setting) | ระเบียบเงินให้กู้ · แบบฟอร์มด้านสินเชื่อ |
| `/deposits/` | `groupDeposits()` | ระเบียบเงินรับฝาก |

**จับคู่ด้วยคำในชื่อทั้งสองฝั่ง** (`WELFARE_TOPICS` · `LOAN_TOPICS` · `DEPOSIT_TOPICS`)
ไม่ได้ผูกด้วยเลขลำดับหรือ `data-*` เพราะ EditUI ไม่มีที่ให้ตั้ง และเจ้าหน้าที่แทรกบรรทัดใหม่
เมื่อไหร่ เลขลำดับจะเลื่อนแล้วผูกผิดทั้งตาราง

- ⚠️ **ใครตรงก่อนได้ก่อน — เรียงจากเฉพาะเจาะจงที่สุดลงมา** (หลักเดียวกับ `DEFS` ใน
  `rateGroups.ts`) หัวข้อกว้าง ๆ อย่าง `สามัญ` / `พิเศษ` ต้องอยู่ล่างสุดเสมอ
  ไม่งั้นมันดูดเกือบทั้งหน้าไปกองอยู่ใบเดียว
- ⚠️ **ห้ามใช้ตัวเลขลอย ๆ เป็นคำจับ** — `65` ไปตรงกับ "พ.ศ. 25**65**" ของระเบียบเงินกู้
  ฉุกเฉิน ต้องเขียน `65\s*ปี` เสมอ (เจอจริงตอนทำหน้าสวัสดิการ)
- ⚠️ **ชื่อเดียวกันสะกดไม่ตรงกันข้ามตาราง** — อัตราเขียน "โซล่าเซลล์" แบบฟอร์มเขียน
  "โซลาเซลล์" ต้องเขียน regex รับทั้งสองแบบ (`โซล่?าเซลล์`)
- **จับไม่ได้ = ไม่หาย** เอกสารที่ไม่เข้าหัวข้อไหนเลย (ระเบียบเงินกู้ฉุกเฉินในหน้าสวัสดิการ ·
  ใบขอรับโอนหนี้ · ระเบียบรับฝากจากนิติบุคคล) ยังอยู่ครบใน `<details>` รายการเต็มท้ายหน้า
  ซึ่งใช้หัวข้อเดิมของตารางเป็นชื่อ — **เพิ่มสินค้า/สวัสดิการใหม่แล้วอยากให้เอกสารไปเกาะการ์ด
  ต้องมาเพิ่มหัวข้อในตารางนี้** ไม่เพิ่มก็แค่ไม่มีเอกสารบนการ์ด ไม่มีอะไรพัง

**หัวข้อ `<h2>` ที่ไม่ใช่ระเบียบ/แบบฟอร์มไม่ถูกแตะเลย** (เช่น "สอบถามเพิ่มเติม")
คงอยู่ที่เดิมตามลำดับเดิม · ระหว่างหัวข้อกับตารางมักมีย่อหน้าอธิบายคั่น ตัวอ่านจึงยอมให้มี
อะไรคั่นได้ แต่ต้องไม่ใช่ `<h2>` อีกอันหรือ `<table>` อีกตาราง ไม่งั้นมันจะกลืนหัวข้อถัดไป

ตรวจของจริงได้โดยดึงเนื้อหาจากฐานมาแล้วสั่งจับคู่ดู — **อย่าตรวจด้วยตาบนหน้าเว็บอย่างเดียว**
เพราะเอกสารที่เกาะผิดเรื่องดูเผิน ๆ เหมือนถูก (ชื่อคล้ายกันหมด)

## หน้าโปรแกรม — หน้าที่เป็นเครื่องมือ ไม่ใช่หน้าไว้อ่าน

`src/lib/programPages.ts` คือทะเบียน · เจ้าของเว็บขอไว้ 25 ส.ค. 2026 ว่าอยากได้หน้าที่
เขียนเป็นโปรแกรมไว้ แล้วได้ที่อยู่ออกมาเอาไปวางตรงไหนก็ได้ที่ต้องการ

เว็บนี้มีหน้าอยู่ **3 แบบ** อย่าสับสนกัน

| แบบ | ทะเบียน | เนื้อหามาจากไหน |
|---|---|---|
| หน้าเนื้อหา | ตาราง `Page` | เจ้าหน้าที่พิมพ์เองใน EditUI |
| หน้าออกแบบอัตโนมัติ | `src/lib/designedPages.ts` | โค้ดจัดหน้าให้ ข้อมูลมาจากหลังบ้าน (เช่น ติดต่อเรา) |
| **หน้าโปรแกรม** | `src/lib/programPages.ts` | **โค้ดล้วน — มีการคำนวณ มีผลลัพธ์ที่คิดสด ๆ** |

⚠️ **เพิ่มโปรแกรมใหม่ต้องต่อท้ายใน `PROGRAM_PAGES` เสมอ** ไม่งั้นจะพังเงียบ ๆ 4 ทาง:
กูเกิลไม่เก็บ · โฮสต์ไม่มีสำเนาไว้เสิร์ฟตอนปิดเครื่อง · **ตัวนับผู้เข้าชมไม่นับหน้านั้น**
(`POST /api/track` รับเฉพาะที่อยู่ที่มีอยู่จริง) · หลังบ้านตอนแก้เมนูขึ้นว่า "ยังไม่มีหน้า"

### โปรแกรมที่ 1 — ตรวจสุขภาพการเงิน (`/tools/financial-checkup/`)

ถาม 21 ข้อทีละข้อ แล้วสรุปว่าเดือนหนึ่งเงินไปไหนหมด · คำถามกับสูตรคิดอยู่ที่
`src/lib/financialCheckup.ts` (ตรรกะล้วน ไม่แตะ React) ส่วนหน้าจออยู่ที่
`src/components/tools/FinancialCheckup.tsx`

- ⚠️ **ห้ามใส่ช่องพิมพ์ตัวเลข** — ทุกข้อตอบด้วยการเลื่อนสเกลอย่างเดียว สมาชิกส่วนใหญ่
  เปิดจากมือถือ พิมพ์เลขยาว ๆ แล้วเลิกกลางคัน · เรื่องนี้ตัดสินใจไปแล้ว ใครขอให้เพิ่ม
  ต้องถามเจ้าของเว็บก่อน
- ⚠️ **ไม่เก็บคำตอบลงฐานเลยสักข้อ** อยู่ใน state ของหน้าล้วน ๆ ไม่มี fetch ไม่มี
  localStorage — เรื่องเงินในบ้านเป็นข้อมูลอ่อนไหวที่สุดที่เว็บนี้จะแตะได้
- สเกลเลื่อนด้วย **ลำดับขั้น** ไม่ใช่จำนวนเงินตรง ๆ (ขั้นถี่ตอนเงินน้อย ห่างตอนเงินเยอะ)
  ถ้าผูกกับจำนวนเงินตรง ๆ ช่วงเงินน้อยจะเลื่อนยากมากบนมือถือ
- **คำถามแก้เองได้ทั้งหมด** — ข้อความ · คำอธิบาย · กลุ่ม · ช่วงเงินของสเกล · ลำดับ ·
  เพิ่ม/ลบข้อ ที่ **หลังบ้าน → หน้าโปรแกรม** เก็บใน `Setting["checkupQuestions"]`
  ยังไม่เคยแก้ = ใช้ชุดตั้งต้นใน `DEFAULT_QUESTIONS` (ชุดที่เจ้าของเว็บเขียนมาเอง 26 ส.ค. 2026)
  · ⚠️ `fillQuestions()` **ต้องถอยกลับไปใช้ชุดตั้งต้นเมื่ออ่านไม่ออก ไม่ใช่คืนรายการว่าง**
  ไม่งั้นแก้ค่าในฐานพลาดทีเดียว หน้าโปรแกรมกลายเป็นหน้าเปล่าทันที
- ⚠️ **21 ข้อที่ถามมีแต่รายจ่าย ไม่มีรายรับ** ผลหลักจึงเป็น "ยอดรายจ่ายรวมต่อเดือน"
  ส่วนคะแนนสุขภาพการเงินคิดได้ต่อเมื่อมีรายรับมาเทียบ — หน้าผลจึงมีสเกลรายรับให้เลื่อน
  เพิ่มทีหลัง **เป็นของแถม ไม่ใช่ข้อบังคับ** อย่าเอาไปยัดเป็นข้อที่ 22
- ภาพประกอบรายข้อเก็บใน `Setting["checkupImages"]` = `{ รหัสคำถาม: ที่อยู่รูป }`
  อัปได้ที่ **หลังบ้าน → หน้าโปรแกรม** · ไม่ใส่ก็ได้ ข้อนั้นจะเป็นแถบสีอ่อนกับไอคอนแทน
  **ความสูงเท่ากันเสมอ** การ์ดคำถามจึงไม่กระตุกตอนเปลี่ยนข้อ
- ⚠️ **เกณฑ์คงเหลือสุทธิ 30%** (`NET_MIN_RATIO`) อ้างอิงแนวทางแก้ไขปัญหาหนี้สินภาครัฐ
  · **ข้อความบนการ์ดเจ้าของเว็บเขียนมาเอง ห้ามแก้ถ้อยคำโดยไม่ถาม** เพราะเป็นการอ้างอิง
  เกณฑ์ราชการ ไม่ใช่คำโปรยที่คิดขึ้นเอง · ฐานคิดคือ **รายได้ − รายจ่ายทั้ง 21 ข้อ**
  (เจ้าของเว็บเลือกเอง 26 ส.ค. 2026) ซึ่งเข้มกว่าเกณฑ์ราชการที่หักเฉพาะรายการหักหนี้
  จึงต้องเขียนสูตรไว้ใต้ตัวเลขเสมอ · **ปัด % ลงเสมอ** 29.6% ห้ามแสดงเป็น 30%
- เบอร์ที่ปุ่ม "โทรหาเจ้าหน้าที่สหกรณ์" มาจาก `Setting["siteInfo"].phone` แอดมินแก้ได้
  ที่ ส่วนท้ายเว็บ — **ห้ามฝังเบอร์ไว้ในโค้ด**
- หน้าสรุปมีสวิตช์ คิด/ไม่คิด รายข้อ กดแล้วทุกอย่างคิดใหม่ทันที · **ปิดแล้วคำตอบเดิม
  ต้องยังอยู่** (เก็บเป็นชุด id ที่ปิด ไม่ใช่ล้างค่าเป็น 0) ไม่งั้นเปิดกลับมาแล้วตัวเลขหาย
- แถบภาพ กราฟแท่ง และวงแหวนคะแนน ทำด้วย CSS ล้วนทั้งหมด ไม่มี chart library
  (หลักเดียวกับกราฟในหน้าภาพรวมหลังบ้าน)

### โปรแกรมที่ 2 — คำนวณดอกเบี้ย (`/tools/interest-calculator/`)

สูตรตามใบประชาสัมพันธ์: **เงินต้น × อัตรา/100 × จำนวนวัน/365** · ตรรกะอยู่ที่
`src/lib/interestCalc.ts` (ไม่แตะ React) หน้าจออยู่ที่ `src/components/tools/InterestCalculator.tsx`

**เดินทีละขั้น 3 ขั้น** (เจ้าของเว็บสั่ง 28 ส.ค. 2026) — กรอกตัวเลข → ผลคำนวณ → สรุป

- ⚠️ **กรอกไม่ครบต้องค้างอยู่ขั้นที่ 1** ห้ามโผล่ผลลัพธ์ครึ่ง ๆ กลาง ๆ · กันสองชั้น:
  ปุ่มกดไม่ได้ **และ** `view` ที่คิดใหม่ทุกครั้งที่ render (`canGo ? step : 1`)
  — **ห้ามแก้เป็น state ตัวที่สองหรือ useEffect** ย้อนไปลบตัวเลขทิ้งแล้วขั้น 2/3 ต้องหายเอง
- เปลี่ยนขั้นด้วย `fadeSwap()` + `STACKED` **ห้ามใส่ `mode="wait"`** (กฎเดียวกับสไลด์ทั้งเว็บ)

**คิดได้สองฝั่ง — เงินกู้ กับ เงินรับฝาก** (เจ้าของเว็บสั่งเพิ่ม 28 ส.ค. 2026)

สูตรเดียวกันเป๊ะ ต่างกันแค่ **ถ้อยคำ · ตารางอัตราที่ดึงมา · คำเตือนท้ายผล**
ถ้อยคำทั้งหมดอยู่ที่ `WORDS` ในไฟล์หน้าจอ — **เพิ่มข้อความใหม่ต้องเพิ่มทั้งสองฝั่ง**
ห้ามเขียนตรง ๆ ใน JSX ไม่งั้นฝั่งเงินฝากจะขึ้นคำว่า "ดอกเบี้ยที่ต้องจ่าย" ซึ่งเข้าใจกลับด้าน

- ⚠️ **คำเตือนเพดานร้อยละ 15 ต่อปีเป็นกฎหมายของเงินกู้เท่านั้น** ห้ามให้ขึ้นในฝั่งเงินรับฝาก
  (`FootNote` แยกให้แล้ว) · ฝั่งเงินฝากบอกข้อจำกัดของการคำนวณแทน
- ⚠️ **ฝั่งเงินฝากคิดแบบไม่ทบต้น และคิดจากยอดคงค้างก้อนเดียว** ของจริงคิดจากยอดคงเหลือ
  รายวันและทบต้นตามรอบจ่ายดอกของสหกรณ์ — ตัวเลขจึงเป็นค่าประมาณที่**ต่ำกว่าของจริงเล็กน้อย**
  ซึ่งปลอดภัยกว่าการโชว์เกิน · เขียนกำกับไว้ท้ายผลแล้ว **ห้ามเอาออก**
- สลับฝั่งแล้ว **ต้องเปลี่ยนอัตราตามฝั่งใหม่ด้วย** (`switchKind`) ไม่งั้นค้างอัตราเงินกู้ 5-6%
  ไว้ในหน้าเงินฝากซึ่งเป็นตัวเลขที่เป็นไปไม่ได้

**เลือกได้ว่าให้ปุ่มลัดอัตราขึ้นประเภทไหนบ้าง** ที่ **หลังบ้าน → หน้าโปรแกรม**

| ฝั่ง | คีย์ใน Setting |
|---|---|
| เงินกู้ | `interestRatesHidden` |
| เงินรับฝาก | `interestDepositHidden` |

- ⚠️ **เก็บฝั่ง "ซ่อน" ไม่ใช่ "โชว์" ตั้งใจ** — เพิ่มประเภทใหม่ที่หน้าอัตราดอกเบี้ยแล้วมันขึ้น
  ในโปรแกรมเองทันที ถ้าเก็บกลับกันประเภทใหม่จะหายเงียบ ๆ โดยไม่มีอะไรฟ้อง
- ⚠️ **ผูกด้วยชื่อรายการ ไม่ใช่ลำดับ** (เจ้าหน้าที่แทรก/สลับแถวได้) — แลกกับว่าเปลี่ยนชื่อ
  รายการที่ติ๊กปิดไว้ การซ่อนจะหลุดแล้วรายการนั้นกลับมาขึ้น หลักเดียวกับสิทธิ์ `page:<หมวด>`
- **ตัวเลขอัตราแก้ที่ หลังบ้าน → อัตราดอกเบี้ย ที่เดียว** หน้าโปรแกรมเลือกได้แค่ขึ้น/ไม่ขึ้น
  ถ้าให้แก้สองที่ ตัวเลขบนหน้าแรกกับในโปรแกรมจะไม่ตรงกันโดยไม่มีใครรู้

## ทางเข้าหลังบ้าน: /login ไม่ใช่ /admin

`/admin` เปิดได้เฉพาะคนที่เข้าระบบแล้ว · ยังไม่ได้เข้า = เด้งไป `/login` ทุกหน้า
(เดิมหน้าล็อกอินอยู่ที่ `/admin` ซึ่งอ่านที่อยู่แล้วเหมือนอยู่ในหลังบ้านทั้งที่ยังไม่ได้เข้า)

`/login` อยู่ใต้ด่านโดเมนเดียวกับ `/admin` ใน `src/proxy.ts` — **เพิ่มเส้นทางหลังบ้านใหม่
เมื่อไหร่ต้องใส่ใน `matcher` ด้วย** ไม่งั้นโดเมนสาธารณะจะเปิดหน้านั้นได้ทั้งที่หลังบ้านซ่อนอยู่

ปุ่ม "เปิดหน้าเว็บไซต์" ในเมนูซ้ายชี้ไปที่ `PUBLIC_SITE_URL` (ไม่ตั้ง = `https://spsccoop.org`)
ชี้ `/` เฉย ๆ ไม่ได้ เพราะหลังบ้านอยู่คนละโดเมนกับหน้าเว็บ มันจะวนกลับมาที่หลังบ้านเอง

## หลังบ้านแบ่งงานตามหน้าที่ — ใครดูแลส่วนไหนก็เห็นแค่ส่วนนั้น

`User.areas` เก็บ "พื้นที่รับผิดชอบ" เป็นรายการข้อความ · **ว่าง = ดูแลได้ทั้งเว็บ**
(ผู้ใช้เดิมทุกคนเป็นแบบนี้ ไม่ได้ตั้งใจตัดสิทธิ์ใครย้อนหลัง) · ADMIN ไม่สนใจค่านี้ เข้าได้ทุกที่เสมอ

คีย์ที่ใช้ได้อยู่ใน `src/lib/permissions.ts` สองแบบ:

| แบบ | ตัวอย่าง | หมายถึง |
|---|---|---|
| เมนูหลังบ้าน | `home.announcements` `holidays` `seo` | เมนูนั้นเมนูเดียว |
| หมวดหน้าเนื้อหา | `page:ดาวน์โหลดเอกสาร` | ทุกหน้าในหมวดนั้น (`pages` = ทุกหมวด) |

หมวดของหน้าเนื้อหาไม่ได้ตายตัวในโค้ด มาจากช่อง `Page.category` ที่เจ้าหน้าที่ตั้งเอง
**เปลี่ยนชื่อหมวด = สิทธิ์ที่ผูกกับชื่อเดิมหลุดทันที** จึงให้เฉพาะคนที่ดูแลหน้าเนื้อหาทุกหมวด
เปลี่ยนชื่อหมวดได้ (ปุ่มดินสอข้างชื่อกลุ่มถูกซ่อนสำหรับคนอื่น)

ด่านกันอยู่สองชั้น ต้องใส่ให้ครบทั้งคู่เวลาเพิ่มหน้า/route ใหม่:

- **หน้าจอ** — `canArea(user, "...")` แล้ว `redirect(ADMIN_HOME)` ต้นไฟล์ `page.tsx`
  (เมนูซ่อนให้อยู่แล้ว ด่านนี้กันคนพิมพ์ที่อยู่เข้ามาเอง)
- **API** — เปลี่ยนจาก `requireUser()` เป็น `requireWrite("พื้นที่")` ทุก route ที่เขียนข้อมูล
  · route ที่สิทธิ์ขึ้นกับของที่กำลังแก้ (หน้าเนื้อหา รายการหน้าแรก) ต้องอ่านของเดิมจากฐาน**ก่อน**
  แล้วค่อยเช็ค — ดู `requireWritePage()` กับ `SECTION_AREA`

`PUT /api/admin/home/` รับค่าตั้งหลายก้อนใน route เดียว (`FIELD_AREA` แมปไว้ว่าก้อนไหนของใคร)
ส่งก้อนที่ไม่ใช่ของตัวเองติดมาด้วย = 403 ทั้งคำขอ ไม่ใช่ข้ามก้อนนั้นเงียบ ๆ

### มุมมองผู้ใช้ — ADMIN สวมมุมมองคนอื่นเพื่อดูว่าเขาเห็นอะไร

กดปุ่มรูปตาในหน้าผู้ใช้งาน · คุกกี้ `spsc_viewas` เก็บ id ของคนที่ถูกสวม
`currentView()` เช็คจาก session จริงทุกครั้งว่าคนกดเป็น ADMIN ไหม **ไม่ได้เชื่อคุกกี้**
เจ้าหน้าที่ธรรมดาแอบตั้งคุกกี้เองจึงไม่มีผล

**มุมมองนี้ดูได้อย่างเดียว** — `requireWrite()` ปฏิเสธทุกคำขอที่ส่งมาระหว่างเปิดมุมมองอยู่
ส่วนคลาส `.view-only` ใน `globals.css` แค่ปิดปุ่มกับช่องกรอกให้เห็นชัดว่าแตะไม่ได้
(ลิงก์ยังกดได้ จะได้เดินดูได้ทั่ว) — **ตัวกันจริงอยู่ที่ฝั่ง API ไม่ใช่ CSS**

ออกจากมุมมองที่แถบสีเหลืองด้านบน — แถบอยู่นอกส่วนที่ถูกปิด กดได้เสมอ ไม่ติดกับดักตัวเอง

## วันหยุดดึงมาจากระบบสำนักงาน ไม่ได้พิมพ์เองสองที่

ต้นทางคือ **QgeniusCoop API** ที่ `http://192.168.100.142:8080/holidays` — คนละเครื่องกับ
เครื่องนี้ (เครื่องนี้คือ `192.168.201.106`) อยู่คนละซับเน็ตแต่เราต์ถึงกันได้
ตั้งที่อยู่ที่ `HOLIDAY_SOURCE_URL` ใน `.env` · ไม่ตั้ง = ซ่อนปุ่มดึงข้อมูลไปเลย

ตอบกลับหน้าตานี้ (ไม่ต้องล็อกอิน · `?year=` ใส่ปีอื่นได้แต่ตอนนี้มีแค่ปีปัจจุบัน):

```json
{ "enabled": true, "holidays": { "2026-04-06": "วันจักรี", ... } }
```

**ดึงมาเก็บในฐานของเว็บ ไม่ได้อ่านสดตอนสมาชิกเปิดหน้าเว็บ** — ต้นทางอยู่ในวงแลน
ปิดเครื่องเมื่อไหร่วันหยุดจะหายจากหน้าเว็บทันที · และดึงมาแล้วยังแก้ชื่อ ใส่หมายเหตุ
หรือซ่อนบางวันได้ตามปกติ ซึ่งอ่านสดทำไม่ได้

**ดึงอัตโนมัติทุก 6 ชม.** ด้วย service `holiday-sync` (เรียก `POST /api/holidays/sync`
ผ่านชื่อ `web:3000` ในเน็ตเวิร์ก Docker) · **เช็คก่อนเสมอ ไม่มีอะไรเปลี่ยนก็ไม่ทำอะไรเลย**
ไม่เขียนฐาน ไม่ล้างสำเนาบนโฮสต์ ไม่ขึ้น log — ขึ้น log เฉพาะตอนมีของใหม่หรือพลาด

เส้นทางนั้นอยู่ **นอก** `/api/admin` ตั้งใจ เพราะ `/api/admin` เปิดได้เฉพาะโดเมนหลังบ้าน
(`src/proxy.ts`) ซึ่ง `web:3000` ไม่ผ่าน · ด่านจึงเป็นโทเคน `HOLIDAY_SYNC_TOKEN` แทน
**ไม่ตั้งโทเคน = เส้นทางตอบ 404 และตัวดึงอัตโนมัติไม่ทำงาน** เหลือแต่ปุ่มกดเอง

ตัวดึงอัตโนมัติ **ไม่ทับชื่อที่เจ้าหน้าที่แก้ไว้เด็ดขาด** (ไม่มีใครนั่งดูว่ามันทับอะไรไป)
ใครอยากทับต้องไปกดเองในหลังบ้าน ซึ่งมีให้ติ๊กยืนยันทีละครั้ง

หลังบ้าน → วันหยุด ยังมีปุ่ม **"ดูรายการ"** แล้วค่อย **"ดึงเข้าเว็บ"** สำหรับตอนอยากได้เดี๋ยวนั้น
— สองขั้นตั้งใจ เพราะต้นทางเป็นระบบของคนอื่น เราไม่ได้คุมว่าวันไหนจะโผล่มา ให้เห็นก่อนว่าจะเปลี่ยนอะไร

| กรณี | ระบบทำอะไร |
|---|---|
| วันที่ยังไม่มีในเว็บ | เพิ่มให้ (เผยแพร่เลย) |
| วันตรงกัน ชื่อตรงกัน | ข้าม |
| วันตรงกัน **ชื่อไม่ตรง** | ไม่แตะ จนกว่าจะติ๊ก "ใช้ชื่อจากระบบสำนักงานทับ" |
| ต้นทางส่ง `enabled: false` | ปฏิเสธทั้งคำขอ ไม่เอาของค้างมาทับ |

เทียบด้วย **วันที่** เป็นหลัก (วันเดียวกัน = วันหยุดเดียวกัน ต่อให้เรียกชื่อต่างกัน)
· ไม่เคยลบวันหยุดที่มีอยู่ในเว็บ ต่อให้ต้นทางไม่มีแล้ว — วันหยุดของสหกรณ์เองที่เพิ่มมือไว้
จะได้ไม่หายไปกับการดึงข้อมูล

**ถามต้นทางทั้งปีนี้และปีหน้า** (`?year=`) พอ ครม. ประกาศวันหยุดปีหน้าช่วง ก.ย.–พ.ย.
แล้วสำนักงานลงตารางไว้ เว็บจะรับมาเอง ไม่ต้องมีใครจำว่าต้องมากดตอนขึ้นปีใหม่
· ปีหน้ายังไม่มีข้อมูลก็แค่ได้ลิสต์ว่าง ไม่ถือว่าพัง

ทดสอบตัวดึงอัตโนมัติได้จากในคอนเทนเนอร์ของมันเอง (ไม่ต้องรอครบ 6 ชม.):

```bash
docker compose exec -T holiday-sync sh -c   'curl -s -X POST -H "x-sync-token: $HOLIDAY_SYNC_TOKEN" http://web:3000/api/holidays/sync/'
```

⚠️ ที่อยู่ต้นทางต้องเป็นที่อยู่ที่ **คอนเทนเนอร์** ต่อถึง ไม่ใช่แค่เครื่องที่เปิดเบราว์เซอร์
(`web` ตั้ง `env_file: .env` ไว้แล้ว ค่าจึงส่งเข้าคอนเทนเนอร์ให้เอง) ตรวจได้ด้วย

```bash
docker compose exec -T web sh -c 'wget -q -O - --timeout=6 http://192.168.100.142:8080/holidays'
```

## รูปที่อัปจากหลังบ้านเก็บที่ไหน

เก็บลง `uploads/` ของเครื่องนี้ **ที่เดียวเท่านั้น** (mount เข้า container ที่
`/app/public/uploads`) · URL ที่บันทึกลงฐานเป็น `/uploads/<uuid>.xxx` เสมอ

ของเดิมส่งสำเนาขึ้น FTP ของโฮสต์ด้วยถ้าตั้ง `FTP_*` + `ASSETS_BASE_URL` ครบ —
**ถอดออกแล้ว 21 ส.ค. 2026 ดูกฎข้อ 2 ด้านบน** · ตรวจแล้วทุกแถวในตาราง `Media` (260 แถว)
ชี้ `/uploads/` ครบ ไม่มีแถวไหนชี้โดเมนโฮสต์เหลืออยู่

`src/lib/assetFallback.ts` กับ `src/lib/assetUrl.ts` ยังอ่าน `ASSETS_BASE_URL` อยู่ —
**ไม่ใช่ของค้าง** เป็นตัวแปลงที่อยู่เก่าให้ชี้กลับมาที่สำเนาในเครื่อง เผื่อวันไหนเจอข้อมูลเก่า
ที่ยังชี้โดเมนที่ตายแล้ว (เคยเกิดกับ `beta.spsccoop.com` ที่หายจาก DNS) · ไม่ตั้งค่า = ไม่ทำอะไร

`next.config.ts` ตั้ง `images.unoptimized: true` ไว้ ภาพจากโดเมนภายนอกจึงใช้ได้เลย
ไม่ต้องประกาศ `images.remotePatterns` — ถ้าวันไหนเปิด image optimization ต้องมาเพิ่มโดเมนตรงนั้นด้วย

`uploads/` ถูกสำรองรายวันแล้วโดย service `db-backup` (ดูหัวข้อสำรองข้อมูลด้านล่าง)

**PDF ที่หนักเกิน 6 MB ถูกบีบด้วย Ghostscript ก่อนเก็บ** — `src/lib/pdf.ts` ไล่ลดความละเอียด
150 → 110 → 90 → 72 dpi หยุดทันทีที่เข้าเป้า (ทดสอบจริง: สแกน 26 หน้า 30 MB → 1.2 MB ใน 6 วินาที)
`gs` ติดมากับ image ผ่าน `apk add ghostscript` ใน Dockerfile — **เครื่องพัฒนา Windows ไม่มี**
ระบบจะข้ามการบีบแล้วเก็บต้นฉบับ ไม่พัง แต่จะทดสอบเรื่องบีบไฟล์ต้องทดสอบในคอนเทนเนอร์

**รูปถูกย่อก่อนเก็บเสมอ** — `src/lib/image.ts` (sharp) ย่อให้ด้านยาวสุดไม่เกิน `MAX_EDGE` = 600px
คงสัดส่วนเดิม ไม่ตัดขอบ (ประกาศมีทั้งแนวตั้งแนวนอน ตัดเป็นจัตุรัสแล้วเนื้อหาหาย)
**เก็บเฉพาะไฟล์ที่ย่อแล้ว ไม่ได้เก็บต้นฉบับ** ย้อนกลับไม่ได้ · GIF ปล่อยผ่านไม่ย่อ (ภาพเคลื่อนไหวเสีย)
`.rotate()` ไว้ก่อน resize เพราะรูปจากมือถือฝังทิศทางไว้ใน EXIF ไม่งั้นได้ภาพตะแคง

ส่วน AI ยังอ่าน **ไฟล์ต้นฉบับ** ที่เบราว์เซอร์ส่งไปอีกทาง ไม่ได้อ่านตัวที่ย่อแล้ว — ตัวอักษรในประกาศ
ที่ย่อเหลือ 600px จะอ่านไม่ออก

Next จำกัดขนาด body ไว้ 10 MB เมื่อโปรเจกต์มี proxy/middleware (โปรเจกต์นี้มี) ไฟล์ใหญ่กว่านั้น
จะถูกตัดกลางทางแล้ว `request.formData()` อ่านไม่ออก — ตั้ง `proxyClientMaxBodySize` ใน
`next.config.ts` ไว้ 70mb แล้ว ถ้าจะขยายเพดานไฟล์อีกต้องขยายค่านี้ตามด้วย

sharp เป็น native module — image เป็น alpine (musl) `npm ci` ในคอนเทนเนอร์จะหยิบ
`@img/sharp-linuxmusl-x64` ให้เอง อย่าก๊อป `node_modules` จากเครื่อง Windows เข้าไป

## ความปลอดภัย — จุดที่เคยพลาดและกันไว้แล้ว

### กันเดารหัสต้องนับตาม "ชื่อผู้ใช้" ด้วย ไม่ใช่แค่ไอพี

รหัสผ่านของระบบนี้คือ **เลข 4 ตัวท้ายเบอร์โทร = ความเป็นไปได้แค่ 10,000 แบบ**
ถ้ายิงรัวได้ก็เดาเจอในไม่กี่นาที ตัวกันเดารหัสจึงเป็นด่านสำคัญที่สุดของระบบ

เดิมนับตามไอพีอย่างเดียว ซึ่ง **ทะลุได้** เพราะไอพีที่เห็นมาจากหัวคำขอ
(`cf-connecting-ip` / `x-forwarded-for`) ที่คนยิงตั้งเองได้ถ้าต่อตรงเข้าเครื่องนี้
โดยไม่ผ่าน Cloudflare — ทดสอบแล้วยิงผิด 7 ครั้งติดโดยเปลี่ยนหัวทุกครั้ง ไม่โดนบล็อกเลย
(20 ส.ค. 2026) ตอนนี้นับ **ทั้งตามไอพีและตามชื่อผู้ใช้** ชั้นหลังตั้งหัวหลอกไม่ได้
เพราะจะเดารหัสของใครก็ต้องส่งชื่อคนนั้นมา

⚠️ **แก้ `src/app/api/auth/login/route.ts` เมื่อไหร่ ต้องทดสอบซ้ำว่ายังกันได้**

```bash
for i in 1 2 3 4 5 6; do
  curl -s -o /dev/null -w "%{http_code}
" -X POST http://localhost:8030/api/auth/login/     -H 'Content-Type: application/json' -H "X-Forwarded-For: 172.16.5.$i"     -d '{"username":"zz-nobody","password":"9999"}'
done   # ต้องได้ 401 ห้าครั้ง แล้ว 429 — ห้ามใช้ชื่อผู้ใช้จริง เดี๋ยวล็อกเจ้าตัว 15 นาที
```

ล็อก 15 นาทีแล้วหายเอง ไม่ต้องให้ใครมาปลด · ผลข้างเคียงที่ยอมรับคือคนแกล้งใส่รหัสผิด
ใส่ชื่อคนอื่นได้ ทำให้เจ้าตัวเข้าไม่ได้ชั่วคราว — ยอมแลกกับการกันเดารหัส

### หัวคำขอด้านความปลอดภัย

ตั้งไว้ที่ `next.config.ts` (`headers()`) ครอบทุกหน้า: `nosniff` · `X-Frame-Options:
SAMEORIGIN` (DENY ไม่ได้ เพราะหน้าแรกหลังบ้าน iframe `/` ของตัวเอง) · `Referrer-Policy` ·
`Permissions-Policy` · **ยังไม่ได้ใส่ CSP ตั้งใจ** — หน้าเว็บมีทั้ง SVG ฝังในหน้า
แผนที่กูเกิล และสไตล์ที่ Next ใส่เอง เปิดแบบเข้มโดยไม่ไล่ทดสอบก่อนจะพังเงียบ ๆ
ถ้าจะเปิดต้องเริ่มจาก `Content-Security-Policy-Report-Only` แล้วดู log ก่อน

### ของที่ปลอดภัยอยู่แล้ว — อย่าไปรื้อ

| เรื่อง | กันไว้ยังไง |
|---|---|
| ฐานข้อมูล | `db`/`db-dev` ผูกกับ `127.0.0.1` เท่านั้น เครื่องอื่นในวงแลนต่อไม่ได้ |
| ตัวเว็บเอง | `web` ผูก `127.0.0.1:8030` ตั้งแต่ 20 ส.ค. 2026 — เข้าจากเครื่องอื่นต้องผ่าน Cloudflare เท่านั้น (`cloudflared` ต่อผ่านชื่อ `web:3000` ในเน็ตเวิร์ก Docker ไม่ได้ใช้พอร์ตนี้) |
| SQL injection | ผ่าน Prisma ทั้งหมด ไม่มีการต่อสตริง SQL เอง |
| SVG ที่อัปมา | `src/lib/svg.ts` ล้างสองรอบ (ตอนอัป + ตอนฝังลงหน้า) ตัด `<script>` และ `on*` |
| หลังบ้านโผล่บนโดเมนสาธารณะ | `src/proxy.ts` ตอบ 404 ถ้าไม่ใช่โดเมนใน `ADMIN_HOST` |
| ไฟล์แนบ | ชื่อไฟล์เป็น UUID เดาไม่ได้ · นามสกุลมาจากรายการที่อนุญาตเท่านั้น |

### เข้าหลังบ้านด้วย LINE Login — ทางหลักแล้ว รหัสผ่านเหลือไว้เข้าครั้งแรก

ตั้งค่าที่ `LINE_CHANNEL_ID` · `LINE_CHANNEL_SECRET` ใน `.env` · **ไม่ตั้ง = ซ่อนปุ่มไปเลย
ระบบเดิมทำงานเหมือนเดิมทุกอย่าง** (เส้นทาง `/api/auth/line/*` ตอบ 404)

```
ครั้งแรก   เข้าด้วยชื่อผู้ใช้ + รหัสผ่าน → บัญชีของฉัน → ผูกบัญชี LINE
ครั้งต่อไป กดปุ่ม LINE อย่างเดียว — รหัสผ่านบนโดเมนสาธารณะใช้ไม่ได้แล้ว
```

**ผูกแล้วรหัสผ่านถูกปิดเอง** (`src/app/api/auth/login/route.ts` ตอบ 403 พร้อม `useLine`)
เพราะรหัสผ่านตั้งต้นคือเลข 4 ตัวท้ายเบอร์ = เดาได้แค่หมื่นแบบ และคนในสำนักงานรู้เบอร์กันหมด
ผูก LINE แล้วไม่มีเหตุผลจะเปิดทางที่อ่อนกว่าค้างไว้

⚠️ **ยกเว้น `localhost` เสมอ** — เข้า `localhost:8030` แล้วยังใช้รหัสผ่านได้ทุกกรณี
หลักเดียวกับ `src/proxy.ts` ที่เปิด localhost ไว้ตลอด: LINE ล่ม · ช่องถูกปิด · ตั้งค่าผิด
ก็ต้องมีทางเข้าที่ไม่พึ่ง LINE **ห้ามเอาข้อยกเว้นนี้ออก** ไม่งั้นวันไหน LINE มีปัญหา
จะไม่มีใครเข้าหลังบ้านได้เลยทั้งสหกรณ์

| ทำอะไรได้ | ใคร |
|---|---|
| ผูก LINE ของตัวเอง | เจ้าตัว (ต้องล็อกอินอยู่ก่อน) |
| ปลดการผูกของตัวเอง | เจ้าตัว **ต้องใส่รหัสผ่านเดิม** |
| ปลดการผูกให้คนอื่น | ADMIN (ปุ่มโซ่ขาดในหน้าผู้ใช้งาน) — ทางรอดเดียวเวลาทำบัญชี LINE หาย |
| สร้างผู้ใช้ใหม่จาก LINE | **ไม่มี** เข้าได้เฉพาะบัญชีที่ผูกไว้แล้วเท่านั้น |

ด่านที่ `/api/auth/line/callback` ต้องผ่านครบ 4 ชั้นถึงจะได้ session:
คุกกี้ของรอบนั้น (ใช้ครั้งเดียว อายุ 10 นาที) · `state` ตรง · `id_token` ผ่านการตรวจของ LINE
(ลายเซ็น · หมดอายุ · `aud` เป็นช่องเรา · `nonce` ตรงรอบ) · และ `lineUserId` นั้นมีคนผูกไว้แล้ว

**`User.lineUserId` เป็น unique ตั้งใจ** — บัญชี LINE หนึ่งอันผูกได้กับเจ้าหน้าที่คนเดียว
ไม่งั้นสองคนใช้ LINE เดียวกันเข้าระบบได้ แล้วดูไม่ออกว่าใครเป็นคนแก้อะไร

⚠️ **Callback URL ต้องมี `/` ปิดท้าย** เพราะ `next.config.ts` ตั้ง `trailingSlash: true`
ที่ใส่ใน LINE Developers ต้องตรงเป๊ะกับ `lineCallbackUrl()` ไม่งั้น LINE ปฏิเสธทั้งคำขอ
· ค่าตั้งต้นเดาจาก `ADMIN_ROOT_HOST` ให้ — **ไม่เคยเดาจากหัว `Host` ของคำขอ** เพราะคนยิง
ตั้งหัวเองได้ แล้วจะพา LINE ให้ส่ง `code` ไปเข้าเครื่องคนอื่น

### รหัสผ่าน — เลข 4 ตัวท้ายเบอร์เป็นแค่ "รหัสตั้งต้น" แล้ว

ทุกคนตั้งรหัสของตัวเองได้ที่เมนู **บัญชีของฉัน** (`/admin/account`) ยาวอย่างน้อย 8 ตัวอักษร
ต้องยืนยันรหัสเดิมด้วย (กันคนมาเปลี่ยนรหัสยึดบัญชีตอนเจ้าตัวลุกจากโต๊ะ)

`User.ownPassword` บอกว่าตั้งรหัสเองแล้วหรือยัง — **เป็น true แล้วระบบจะเลิกเขียนทับ
ด้วยเลข 4 ตัวท้ายเบอร์** ถึงจะไปแก้เบอร์โทรทีหลังก็ตาม (ไม่งั้นรหัสที่ตั้งเองหายเงียบ ๆ)

| ใคร | ทำอะไรได้ |
|---|---|
| เจ้าตัว | ตั้งรหัสของตัวเอง (ต้องใส่รหัสเดิม) |
| ADMIN | **ตั้งรหัสให้คนอื่นโดยตรงไม่ได้** — ทำได้แค่ปุ่มรูปกุญแจ "ตั้งรหัสใหม่จากเบอร์โทร" ดีดกลับไปเป็นรหัสตั้งต้น แล้วบอกเจ้าตัวไปตั้งใหม่ |

ที่ ADMIN ตั้งรหัสให้คนอื่นไม่ได้เพราะไม่ควรมีใครรู้รหัสของคนอื่น · ในหน้าผู้ใช้งาน
มีป้ายเหลือง "ยังใช้รหัสตั้งต้นจากเบอร์โทร" ให้เห็นว่าใครยังไม่ได้ตั้ง

### เนื้อหาหน้าเว็บถูกกรองทุกครั้งที่บันทึกแล้ว

เนื้อหาขึ้นจอด้วย `dangerouslySetInnerHTML` ถ้าบัญชีเจ้าหน้าที่คนไหนหลุด คนที่ยึด
บัญชีได้จะฝัง `<script>` ลงหน้าเว็บแล้วยิงใส่สมาชิกทุกคนที่เปิดหน้านั้น —
เดิม `cleanPageHtml` ทำงานเฉพาะกับผลจาก AI **ตอนนี้ใช้ตอนบันทึกทุกทางแล้ว**
(`PATCH /api/admin/pages/[id]`) ตัด `<script>` · `on*` · `javascript:` · `style` ที่ไม่ใช่ `--pdf-size`

⚠️ **แก้ `ALLOWED_CLASSES` / `ALLOWED_ATTRS` เมื่อไหร่ ต้องรันตัวตรวจ** ไม่งั้นตัวกรอง
จะกินของที่เขียนไว้แล้วหายไปเงียบ ๆ ตอนมีคนกดบันทึก

```bash
npm run check:filter -- pages.json out.txt      # ต้องผ่านครบทุกหน้า
```

ตัวตรวจนี้จับได้แล้วหนึ่งตัว: `ebook-name` (ชื่อไฟล์บนการ์ด PDF) ไม่ได้อยู่ใน
`ALLOWED_CLASSES` มาตั้งแต่แรก — ถ้าเปิดตัวกรองโดยไม่ตรวจก่อน ชื่อไฟล์บน 3 หน้า
จะกลายเป็นข้อความเปล่าทันทีที่มีคนกดบันทึก

ผลข้างเคียงที่ยอมรับ: หมายเหตุ `<!-- ... -->` ในโค้ดถูกตัดทิ้งตอนบันทึก

### ตัวนับเข้าชมรับเฉพาะที่อยู่ของหน้าที่มีอยู่จริง

`POST /api/track` เปิดให้ยิงได้โดยไม่ต้องล็อกอิน (หน้าเว็บต้องยิงเองได้) เดิม
**รับที่อยู่อะไรก็ได้** ใครยิงที่อยู่มั่ว ๆ ก็สร้างแถวใน `PageView` ได้ไม่จำกัด
ฐานบวม ไฟล์สำรองใหญ่ ตัวเลขบนหน้าเว็บเพี้ยน และหน้ายอดนิยมในหลังบ้านเต็มไปด้วยขยะ
(ทดสอบแล้วยิงเข้าจริงทั้ง `/../../etc/passwd` และ `/zz-<script>` เมื่อ 20 ส.ค. 2026)

ตอนนี้เทียบกับ `publicPaths()` ก่อนเสมอ ที่ไม่ตรงคือไม่นับ · เก็บรายชื่อไว้ในหน่วยความจำ
5 นาที จะได้ไม่ถามฐานทุกครั้งที่มีคนเปิดหน้า · `/read` กับ `/ebook/<id>` ยกเว้นให้
เพราะสร้างที่อยู่จากรหัสไฟล์ ไม่ได้อยู่ในรายการหน้าเว็บ

**เพิ่มหน้าแบบใหม่ที่มีตัวแจ้งนับติดอยู่ ต้องเพิ่มใน `publicPaths()` หรือในข้อยกเว้นด้วย**
ไม่งั้นหน้านั้นจะไม่ถูกนับเลยโดยไม่มีอะไรฟ้อง

## ไฟล์ที่ไม่อยู่ใน git — ต้องก๊อปมือเวลาย้ายเครื่อง

`.env` (มี `TUNNEL_TOKEN` ตัวจริง) · `backups/*.sql` · `uploads/` · `_data/`
`.env.example` ติด repo ไว้ให้รู้ว่าต้องตั้งตัวแปรอะไรบ้าง

## สำรองข้อมูล

service `db-backup` ทำงานทุก 24 ชม. เก็บย้อนหลัง 14 วัน ลง `backups/`

| ไฟล์ | คืออะไร |
|---|---|
| `coopsmile-YYYY-MM-DD.sql` | ฐานข้อมูล |
| `uploads-YYYY-MM-DD.tar.gz` | รูปที่อัปจากหลังบ้าน |

ต้องกู้ทั้งสองอย่างคู่กัน — กู้แต่ฐานจะเหลือ path ที่ชี้ไปไฟล์ที่ไม่มีอยู่ หน้าเว็บรูปแตก

```bash
# กู้คืน
docker compose exec -T db psql -v ON_ERROR_STOP=1 -U coopsmile coopsmile < backups/ไฟล์.sql
tar -xzf backups/uploads-ไฟล์.tar.gz -C uploads/
```

`migration/import-new-machine.sh` ทำขั้นตอนกู้ทั้งชุดให้ (สำรองของเดิมไว้ก่อนแล้วค่อยทับ)

## เรื่องจุกจิกของ shell บนเครื่องนี้

**ห้ามส่งข้อความภาษาไทยผ่านบรรทัดคำสั่ง** — เชลล์บนเครื่องนี้แปลงเป็นรหัส cp874 ระหว่างทาง
ข้อมูลจะเสียแบบเงียบ ๆ ไม่มี error ให้เห็น เช่น

```bash
curl -X PATCH -d '{"title":"ดาวน์โหลดเอกสาร"}' ...     # ผิด ได้ข้อความเพี้ยนลงฐาน
docker compose exec -T db psql -c "UPDATE ... 'ไทย'"    # ผิดเหมือนกัน
```

เคยพลาดมาแล้ว 19 ส.ค. 2026 ตอนทดสอบระบบล้างแคช ชื่อหน้า 2 หน้าเพี้ยนขึ้นเว็บจริง
กว่าจะรู้ตัวคือเจ้าของเว็บทักมา (ในเทอร์มินัลมองไม่ออกเพราะมันเพี้ยนอยู่แล้วเป็นปกติ)

**เวลาให้ Python พิมพ์ภาษาไทยออกจอ ต้องตั้ง `PYTHONIOENCODING=utf-8` ก่อน**
ไม่งั้นได้ตัวอักษรขยะอ่านไม่ออก (ส่วน `echo` ของ bash กับ `psql` แสดงไทยได้อยู่แล้ว)

```bash
export PYTHONIOENCODING=utf-8
python -c "print('ดาวน์โหลดเอกสาร')"
```

**วิธีที่ถูก** — เขียนไฟล์ด้วย Python ระบุ UTF-8 แล้วค่อยป้อนเข้าไป

```bash
python -c "import io; io.open('a.sql','w',encoding='utf-8').write(\"UPDATE ...'ไทย'...\")"
docker compose exec -T db psql -U coopsmile coopsmile < a.sql
```

**กฎเดียวกันใช้กับข้อความ commit ภาษาไทย** — `git commit -m "ไทย"` หรือ heredoc `-F -`
เพี้ยนเป็น cp874 ทั้งคู่ (พลาดมาแล้ว 20 ส.ค. 2026 ต้อง amend แก้) ให้เขียนข้อความลงไฟล์
ด้วยเครื่องมือที่เขียน UTF-8 ตรง ๆ แล้วค่อย `git commit -F ไฟล์.txt`
ตรวจว่าเพี้ยนไหม: `git log -1 --pretty=%B` แล้วลอง decode utf-8 ถ้า throw คือเพี้ยนแล้ว

ตรวจว่าเสียหรือไม่: `select char_length(title), octet_length(title)` ภาษาไทยที่ถูกต้อง
ไบต์จะราว 3 เท่าของจำนวนตัวอักษร ถ้าใกล้เคียงกันแปลว่าเพี้ยนแล้ว

รันสคริปต์ด้วย **Git Bash** — PowerShell 5.1 ไม่รองรับ `<` (input redirection) คำสั่ง restore
ฐานข้อมูลจะฟ้อง `The '<' operator is reserved` ถ้าเลี่ยงไม่ได้ให้ห่อด้วย `cmd /c "... < file.sql"`
(อย่าใช้ `Get-Content | ...` ภาษาไทยใน dump จะเพี้ยน)

## พอร์ตที่ระบบนี้จอง

`8030` เว็บจริง · `5435` db · `5436` db-dev · `3000` `npm run dev`
